# AGENTS.md

## Repo Shape
- On branch `feature/xband-only-runtime`, `SpeedifyUi.sln` contains two .NET 9 projects: `XNetwork/XNetwork.csproj` (Blazor Server dashboard) and `XNetwork.Tests/XNetwork.Tests.csproj` (xUnit tests). `SpeedifyProbeAgent` is intentionally removed from this branch.
- Main app wiring is in `XNetwork/Program.cs`: Interactive Server components, route transitions, `XBondStatsService`, `XBondStatusService`, `XBondTrafficEngineService`, `NetworkMonitorService`, `ConnectionHealthService`, `CudyApControlService`, `XRouterService`, and `LocalProcessTrafficService`.
- Main runtime routes on `feature/xband-only-runtime` are `/` (`Home.razor`), `/details` (`Statistics.razor`), `/xbond`, `/settings`, and `/xrouter` (`XRouter.razor`, shown in the UI as `Wifi`). Legacy controls/server-switching pages are intentionally removed.
- Product branding is `XNetwork`; the historical `/xrouter` route and some class names remain for compatibility and should not be renamed casually.
- Frontend assets are mostly static/CDN: Tailwind, Font Awesome, Google Fonts, and Chart.js are loaded from CDNs in `App.razor`; local JS modules live in `XNetwork/wwwroot/js`.
- Current app logo asset is `XNetwork/wwwroot/icons/xnetwork-logo.png`; browser/PWA references use `/icons/xnetwork-logo.png?v=20260511`.

## Commands
- Build: `dotnet build SpeedifyUi.sln`
- Test: `dotnet test XNetwork.Tests/XNetwork.Tests.csproj`
- Run locally: `dotnet run --project XNetwork/XNetwork.csproj`
- Publish app: `dotnet publish XNetwork/XNetwork.csproj -c Release`
- Rust tests: run `cargo test` from `xbond/`.
- Real app HTTP binding is `http://0.0.0.0:8080` from `XNetwork/appsettings.json`; `launchSettings.json` ports are not the deployment binding.
- If parallel `dotnet build` and `dotnet test` collide on `obj` files, run `dotnet build-server shutdown` and rerun sequentially.

## Git And Workspace Notes
- The Speedify-based production history used `bugfix/settings-dropdown-refresh`; the XBond-only runtime experiment uses `feature/xband-only-runtime`. Verify with `git status`/`git branch` before assuming.
- Untracked `scripts/` has been present and unrelated; do not add, delete, or modify it unless the user explicitly asks.
- Local newline-only noise has appeared in `XNetwork/wwwroot/js/pwa.js` and `XNetwork/wwwroot/service-worker.js`; avoid committing those unless their content is intentionally changed.
- Do not commit secrets such as Cudy admin passwords, probe API keys, private SSH keys, runtime `appsettings.json`, or files under `/home/xeon-network/.config/XNetwork`.

## Deployment
- Router host is `xeon-network`; the app also responds on Tailscale IP `100.112.183.104` when Tailscale is healthy.
- From this workstation, SSH has used `C:\Users\Xeon\.ssh\speedifyui_cli_probe`; never commit key material.
- Router repo/deploy path is `/home/xeon-network/xnetwork`; service is `xnetwork.service`.
- Deploy command used from Windows: `ssh -i "C:\Users\Xeon\.ssh\speedifyui_cli_probe" xeon-network@xeon-network "cd /home/xeon-network/xnetwork; ./deploy.sh"`
- `deploy.sh` runs `git pull`, preserves published `appsettings.json` and `auto-server-switch-state.json`, cleans publish output, runs `dotnet publish XNetwork/XNetwork.csproj -c Release`, restores preserved runtime files, kills the `xnetwork.service` MainPID, and lets systemd restart it.
- `deploy.sh` also attempts to create `/var/lib/xnetwork/diagnostics` owned by the deploy/service user. The app falls back to the service user's local diagnostics path if `/var/lib/xnetwork` is not writable.
- The current `deploy.sh` uses MainPID `kill -KILL` to trigger the `xnetwork.service` restart. As of 2026-06-15, `xeon-network` has passwordless sudo (`sudo -n true` succeeds), but keep the deploy flow unchanged unless deliberately updating service restart behavior.
- After deploy, verify with `systemctl is-active xnetwork.service`, `curl -sS -o /dev/null -w '%{http_code}\n' http://127.0.0.1:8080/`, and optionally external `curl http://100.112.183.104:8080/`.
- Remote publish warnings should be reviewed normally on `feature/xband-only-runtime`; the old `SpeedifyService.cs` warning note no longer applies because that service is removed in this branch.
- When changing XBond protocol, scheduler, status schema, config schema, service templates, routing behavior, or any shared Rust code used by both ends, deploy the matching `xbond-client` binary to `xeon-network` and `xbond-server` binary to `xeon-speedify-vultr-01` in the same work item. Do not leave client/server protocol or runtime behavior mismatched. After deployment, verify `xbond-client.service`, `xbond-server.service`, `xbond-server-nat.service`, app HTTP routes, `ip route get 8.8.8.8`, `ping -I xbond0 10.250.0.1`, and an internet ping through `xbond0`.
- Preferred paired runtime deploy command from Windows is `.\deploy-xbond-paired.ps1`. Use the router-side `./deploy.sh` only for app-only changes that do not affect the Rust XBond runtime.

## XBond-Only Branch
- 2026-06-15: Branch `feature/xband-only-runtime` is the XBond-only runtime branch. App version source of truth is `AppChangelog.CurrentVersion`; it started at `xbond-2026.06.18` and has continued with `xbond-` prefixed changelog revisions.
- 2026-06-15: This branch intentionally has no Speedify/XBond toggle, no canary mode, no `speedify_cli` polling, no private reconnect, no auto server switching, no Speedify transport/bonding settings, no Speedify adapter encryption/rate-limit controls, and no `SpeedifyProbeAgent` project reference.
- 2026-06-15: Dashboard and `/details` analytics read XBond runtime state through `XBondStatsService` and `XBondStatusService`, which read `/run/xbond/client-status.json` directly. Dashboard upload/download use XBond tunnel payload throughput counters, not duplicated per-path egress totals.
- 2026-06-15: Rust command `xbond-client tunnel` replaces the old canary tunnel command. Deployment templates are `xbond-client.service`, `xbond-server.service`, and `xbond-server-nat.service`; service files should not use canary naming on this branch.
- 2026-06-15: XBond tunnel scheduling recomputes roles every 1 second. Paths with `NO-CARRIER`/down interfaces, socket bind/connect failure, repeated send failures, missing live sockets, or full-loss health are put in cooldown/unavailable state and cannot be anchor.
- 2026-06-15: XBond-only production routing remains IPv4-only for this milestone. No automatic rollback feature is implemented on this branch; scoped `/32` route diagnostics are operator tooling rather than normal user-facing UI.
- 2026-06-15: Live `xeon-network` Wi-Fi is `wlan0` connected to SSID `XNetwork Wi-Fi Asia`, but production `/etc/xbond/client.toml` currently configures only Starlink, Smart, Dito, and Globe as XBond paths. `XBondStatsService` may surface connected ethernet/wifi interfaces that are not configured as synthetic dashboard rows with `not in XBond`; they must not be treated as tunnel paths until added through Settings or directly to `/etc/xbond/client.toml` and `xbond-client.service` is restarted.
- 2026-06-15: XNetwork version `xbond-2026.06.19` adds Settings UI controls for XBond adapter membership. `XBondClientConfigService` reads `/etc/xbond/client.toml`, merges live NetworkManager adapter metadata, writes config changes with `sudo -n /usr/bin/tee`, and restarts `xbond-client.service`; live verification showed `wlan0` / `XNetwork Wi-Fi Asia` as `Available` with an `Add` button, but it was not auto-added.
- 2026-06-15: XNetwork version `xbond-2026.06.20` changes the Heartbeat diagnostic to use route-aware `xbond-client multi-ping` against the selected live path instead of the older single-socket `xbond-client ping`. Live server logs showed the prior heartbeat packet-loss warning was misleading: the server received and ACKed all heartbeat packets, while the single-ping client missed ACKs under the live XBond/default-route setup. The stale installed Rust binaries on `xeon-network` and `xeon-speedify-vultr-01` were rebuilt/replaced during this investigation.
- 2026-06-15: XNetwork version `xbond-2026.06.21` adds a manual Tunnel Speed Test card on `/xbond`. It runs `speedtest-cli --secure --json` on `xeon-network`, parses speedtest-cli bit/s JSON into Mbps, and shows whether the default route was using `xbond0` when the test started.
- 2026-06-15: XNetwork version `xbond-2026.06.22` splits XBond per-path throughput into inbound/download and outbound/upload counters. `throughput_bps` remains total throughput for compatibility, while `inbound_throughput_bps` and `outbound_throughput_bps` drive the dashboard, `/xbond`, and `/details` path displays.
- 2026-06-15: XBond tunnel-local iperf diagnostics use `xbond-iperf3.service` on `xeon-speedify-vultr-01`, bound to `10.250.0.1:5201/tcp`, with a UFW allow rule only from Pi tunnel IP `10.250.0.2` on `xbonds0`. Point-in-time Pi-to-Vultr tunnel tests measured about 18.2 Mbps upload and 17.8 Mbps download, matching the slow public speedtest range and pointing to the XBond tunnel/dataplane path rather than weak Wi-Fi RF.
- 2026-06-15: XNetwork version `xbond-2026.06.23` clarifies dashboard adapter throughput labels by showing explicit `Down` and `Up` text beside each per-path value.
- 2026-06-15: XNetwork version `xbond-2026.06.24` fixes XBond return scheduling and path receive accounting. `xbond-client tunnel` sends a sealed control frame with the live schedule once per scheduler tick; `xbond-server` uses that schedule for return traffic instead of sending to every known peer sorted by path ID. Per-adapter `Down` now reports raw adapter receive load, including duplicate return frames that arrive late and are discarded, while the connection summary still reports useful tunnel download only.
- 2026-06-15: Bounded throughput isolation showed hardware is not the primary Wi-Fi bottleneck: direct native `wlan0` iperf to Vultr measured about 185 Mbps upload and 163 Mbps download, while Wi-Fi-only XBond measured about 49 Mbps upload and 63 Mbps download. Direct modem paths were much slower at that point: Smart about 2.9 Mbps upload / 9.1 Mbps download and Dito about 0.3 Mbps upload / 1.4 Mbps download.
- 2026-06-15: XBond slowdown investigation found the largest initial dataplane bottleneck was per-packet work in long-running services: `xbond-client.service` and `xbond-server.service` ran with `--json-events`, emitted packet-level journald logs, and the client rewrote `/run/xbond/client-status.json` after packet activity. Packet logs are now gated behind `--trace-packets`, and runtime status writes happen on scheduler ticks instead of per packet.
- 2026-06-15: XBond UDP receiver tasks previously exited permanently after transient `recv`/`recv_from` errors, leaving affected path sockets undrained while the scheduler still considered paths present. Client and server receiver loops now log the transient error, sleep briefly, and continue.
- 2026-06-15: XBond health/control frame duplicate detection was separated from data/FEC duplicate classes, and client heartbeat sequence IDs are path-scoped. This prevents health frames from being discarded because a data/control frame reused the same sequence number class.
- 2026-06-15: XBond duplicate and FEC sends are now best-effort nonblocking for backup/parity traffic, while primary data sends remain awaited. This prevents a slow or congested redundant path from directly blocking the anchor send loop during throughput tests.
- 2026-06-15: Post-fix live throughput improved substantially but is not yet near-native in all cases: duplicate mode measured about 105-130 Mbps download and 67-76 Mbps upload in bounded samples; single-active-path testing reached about 113 Mbps upload. Remaining performance work should focus on reorder/dejitter buffering, adaptive duplicate-vs-anchor-only policy for bulk flows, less per-packet allocation/encryption overhead, batching/zero-copy where feasible, and congestion scoring that demotes slow backups under sustained load.
- 2026-06-15: XBond adaptive redundancy policy, return-path reorder buffering, and degraded-backup simulation were recovered from interrupted thread `019e87f9-e3d2-7041-95a2-cc2079af633f` and committed on `feature/xband-only-runtime` as `a1a571a` plus follow-up build cleanup `7ba234d`. The app version for that milestone is `xbond-2026.06.25`.
- 2026-06-15: XBond return-path outage after a server-only rebuild was caused by `xbond-server` restarting its reverse packet sequence at zero while the still-running client reorder buffer expected a higher sequence. Commit `f85c86b` initializes the server reverse sequence from current microsecond time to avoid restart rewinds; it was deployed to Vultr and the router repo, with deployed build info showing version `xbond-2026.06.25`, branch `feature/xband-only-runtime`, commit `f85c86b`.
- 2026-06-15: Post-`f85c86b` live verification on `xeon-network` showed default IPv4 route through `xbond0`, `ping -I xbond0 10.250.0.1` passed 5/5 at about 36 ms average, `ping 1.1.1.1` passed 10/10 at about 40 ms average, GitHub HTTPS returned HTTP 200 through XBond, and app routes `/`, `/details`, `/xbond`, `/settings`, and `/xrouter` returned HTTP 200.
- 2026-06-15: XNetwork version `xbond-2026.06.26` changes the dashboard connection-health latency probe from Cloudflare DNS `1.1.1.1` to Google DNS `8.8.8.8`.
- 2026-06-16: Bounded `tc netem` simulation on `xeon-network` scoped impairment to outbound XBond UDP traffic for `45.77.241.247:8444` only; artifact `/tmp/xbond-netem-summary-20260616T074351Z.json` showed baseline tunnel iperf about 143 Mbps up / 136 Mbps down, degraded active backup still 0% ping loss and about 166 Mbps up / 142 Mbps down, full anchor outage still 0% ping loss after role rotation but latency spikes to about 286 ms max, all-live intermittent paths degraded to about 8.3% tunnel ping loss and 0 Mbps up / 8.2 Mbps down, and severe all-path impairment lost about 46.7% pings. Cleanup was verified: live interfaces returned to normal `fq_codel`, `xbond-client.service` stayed active, default route remained `xbond0`, and post-test `8.8.8.8` ping passed 5/5 at about 36.8 ms average.
- 2026-06-16: XBond version `xbond-2026.06.37` commit `9f4d9ad` adds full-redundant recovery for `Balanced` and `Reliable` policies. When every usable live path is degraded for the configured entry window, XBond temporarily treats transmission as `Reliable` and expands the schedule to duplicate all traffic, including bulk TCP, across all eligible non-hard-demoted paths. Recovery exits after a clean path remains healthy for the configured exit window.
- 2026-06-16: XBond full-redundant recovery commit `9f4d9ad` was paired-deployed to `xeon-speedify-vultr-01` and `xeon-network`; `xbond-server.service`, `xbond-server-nat.service`, `xbond-client.service`, and `xnetwork.service` verified active, app routes `/`, `/xbond`, `/details`, `/settings`, `/xrouter`, and `/wifi` returned HTTP 200, route to `8.8.8.8` used `xbond0`, and pings to `10.250.0.1` plus `8.8.8.8` passed.
- 2026-06-16: Post-recovery bounded `tc netem` simulation artifact `/tmp/xbond-netem-summary-20260616T094536Z.json` showed baseline about 135 Mbps up / 137 Mbps down with 0% tunnel loss, active-backup degradation about 145 Mbps up / 129 Mbps down with 0% tunnel loss and recovery inactive, anchor outage with recovery active at 0% tunnel/public ping loss but about 92 ms tunnel average, all-live intermittent with recovery active but still poor at 25% tunnel ping loss / 41.7% public ping loss / 0 Mbps upload, and severe all-path impairment with only one eligible path and about 93% tunnel ping loss. After 35 seconds of cleanup/settle, recovery exited to inactive, qdiscs were back to `fq_codel`, route remained `xbond0`, and pings to `10.250.0.1` and `8.8.8.8` passed 5/5. This proves recovery state transitions work, but simultaneous intermittent-path performance still needs more work.
- 2026-06-16: Focused all-live-intermittent capture artifact `/tmp/xbond-collapse-capture-20260616T120334Z.json` showed tunnel ICMP mostly surviving at 60 sent / 59 received / 1.67% loss but TCP upload collapsing to about 0.07 Mbps with 45 retransmits. Client process/TUN/inbound queues were not saturated (`tun_queue_depth=0`, inbound queue max 2, queue drops 0, duplicate send skips unchanged), so the collapse is not primarily client queue pressure or CPU/RSS growth.
- 2026-06-16: The same focused capture showed XBond server return-schedule updates flapping while recovery was active, repeatedly switching duplicate order and temporarily dropping one duplicate path. The server code currently clamps ingress reorder hold and tick to at most 50 ms even though the service is configured with `--realtime-deadline-ms 500`; this is likely too short for heavy intermittent paths with 140-360 ms delay plus jitter and can expose packet reordering/loss to TCP. Next runtime work should add server ingress reorder telemetry/status and test a longer/adaptive reorder hold before changing production defaults.
- 2026-06-16: XNetwork/XBond version `xbond-2026.06.38` commit `ffb08d4` implements the intermittent-path collapse fix step: `xbond-client` includes `recovery_active` in schedule-control messages, recovery duplicate path membership/order is held stable for 5 scheduler ticks unless a path is hard-demoted, and `xbond-server` uses explicit ingress reorder flags `--ingress-reorder-normal-hold-ms 50`, `--ingress-reorder-recovery-hold-ms 500`, and `--ingress-reorder-capacity 8192`.
- 2026-06-16: `xbond-server.service` now writes operator status to `/run/xbond/server-status.json`, including return schedule, recovery state, ingress reorder hold, pending depth, held packets, gap/timeout/capacity releases, late duplicates, and forwarded packet counters. This is operator tooling only and is not shown in the normal UI.
- 2026-06-16: Operator capture script `xbond/tools/recovery_ab_capture.py` runs on `xeon-network` with sudo to apply bounded `tc netem` to live physical XBond interfaces, sample `/run/xbond/client-status.json`, run tunnel ping and iperf upload/download to `10.250.0.1:5201`, capture qdisc state, and clean qdiscs afterward. It intentionally stays outside the user-facing app.
- 2026-06-16: Commit `ffb08d4` was paired-deployed to `xeon-speedify-vultr-01` and `xeon-network`; `xbond-server.service`, `xbond-server-nat.service`, `xbond-client.service`, and `xnetwork.service` verified active, app routes `/`, `/xbond`, `/details`, `/settings`, and `/wifi` returned HTTP 200, route to `8.8.8.8` used `xbond0`, and `ping -I xbond0 10.250.0.1` passed 3/3 at about 35 ms average. Temporary A/B service override was removed afterward and `xbond-server.service` returned to normal/recovery hold `50/500 ms`; router qdiscs returned to normal `fq_codel`/empty state.
- 2026-06-16: All-live-intermittent A/B artifacts from `xeon-network` are `/tmp/xbond-recovery-capture-1781619259.json` (50 ms hold), `/tmp/xbond-recovery-capture-1781619291.json` (150 ms), `/tmp/xbond-recovery-capture-1781619332.json` (250 ms), and `/tmp/xbond-recovery-capture-1781619363.json` (500 ms). Tested live paths were `wlan0` / XNetwork Wi-Fi Asia, `enx103c59f1039c` / Smart, and `enxb8d4bcbcb0f0` / Dito.
- 2026-06-16: A/B result summary: 50 ms hold had 40/36 tunnel pings, 0.13 Mbps upload with 26 retransmits, and server timeout releases/late duplicates 1918/1011; 150 ms had 40/39 pings, 0.26 Mbps upload with 14 retransmits, and timeout/late 18/14; 250 ms had 40/39 pings, 0.26 Mbps upload with 10 retransmits, 1.73 Mbps download, and timeout/late 42/31; 500 ms had 40/40 pings, 0.79 Mbps upload with 8 retransmits, and timeout/late 10/0. Longer recovery hold clearly reduced server reorder timeouts and TCP retransmits, with 500 ms best in this run, but all-live intermittent TCP throughput is still far below normal and needs further recovery-path work.

## Historical Speedify Branch Notes
- The notes in this section describe the pre-`feature/xband-only-runtime` Speedify-based branch history. Do not use them as implementation instructions on `feature/xband-only-runtime`; that branch intentionally removes the Speedify runtime dependency and must not call `speedify_cli`.
- Older Speedify-based app revisions required `speedify_cli` in PATH; successful commands return JSON and errors are on stderr as documented in `speedify-cli.md`.
- All Speedify process execution belongs in `SpeedifyService`: terminating commands use `RunTerminatingCommand`, streaming stats use `StreamCommandOutputAsync("stats")`.
- Streaming stats must buffer until a complete JSON `[]` array parses; keep the 32KB runaway safeguard and `Kill(true)` plus 2-second wait cleanup for canceled streams.
- `GetStatsAsync` yields only per-connection `connection_stats` and intentionally filters the `speedify` aggregate plus `%proxy` connections; `GetStatsWithAggregateAsync` includes the `speedify` aggregate for dashboard actual throughput.
- Units differ by API: live Speedify 16.8.0 `ConnectionItem.ReceiveBps`/`SendBps`/`TotalBps` from `stats` match bits/sec in sustained OS/F50 counter tests despite the `Bps` JSON names and older docs; convert those fields to Mbps with `/ 1_000_000`. `show adapters` rate-limit fields `downloadBps`/`uploadBps` are bytes/sec, `speedify_cli adapter ratelimit` command inputs are bits/sec, adapter data usage/limits are bytes, and `0` means unlimited.
- Dashboard server card displays compact Speedify transport and bonding-mode metadata beside the server location. Use `SpeedifySettings.TransportMode` for the configured mode, `SpeedifySettings.BondingMode`/`MaxRedundant` for mode text such as `Redundant x2`, and live connected non-proxy `ConnectionItem.Protocol` stats for the actual protocol when configured mode is `auto`; avoid using aggregate `speedify` or `%proxy`/`%share` rows for active transport detection. Keep transport out of `ConnectionSummary` so the main status card remains a three-metric layout.
- Dashboard adapter cards sort by rolling adapter health via `AdapterHealthPresenter`, not raw instantaneous ping. Treat `0 ms` / missing stats as unknown instead of excellent, because Speedify can report unmeasured adapters with zero latency.
- Direct Starlink dish telemetry is provided by `StarlinkTelemetryService`, which polls the Starlink management API at `192.168.100.1:9200` using `SpaceX.API.Device.Device/Handle` gRPC/h2c and can fall back to gRPC-Web on `9201`. The dashboard Starlink row uses this direct dish data for dish latency, throughput, obstruction, alignment, uptime, GPS, and alerts; do not substitute Speedify `ispStats` for these values.
- Starlink adapter detection must not hardcode USB adapter IDs. XNetwork detects Starlink adapters from live Speedify metadata (`Starlink` or `ispType=Satellite`), a gateway matching the configured Starlink management host, or a periodic interface-bound HTTP probe to `192.168.100.1` using `curl --interface <adapter>`. This keeps detection resilient when USB port/order changes produce new adapter IDs.
- 2026-06-07: Dashboard Starlink adapter cards open a Starlink details `ActionSheet` with direct dish telemetry, bounded live rolling graphs, device/software details, active alerts, and detected local Starlink web/API capabilities. The app links to the local Starlink UI for available controls instead of issuing destructive reboot/stow/unstow commands directly.
- 2026-06-07: Direct Starlink app controls were added for confirmed capabilities. The live Starlink web bundle showed `Request.reboot` field `1001`, `Request.dish_stow` field `2002`, and `DishStowRequest.unstow` bool field `1`; XNetwork sends those commands over the local Starlink gRPC-Web endpoint `192.168.100.1:9201` only after a confirmation modal.
- 2026-06-07: Direct Starlink reset obstruction map support uses gRPC-Web command `dish_clear_obstruction_map` / `DishClearObstructionMapRequest` on request field/tag `2017`. This clears the learned obstruction map, may take hours or days to rebuild, and is gated by typed confirmation phrase `CLEAR MAP`; do not execute it unless the user explicitly asks.
- 2026-06-07: XNetwork app versioning now uses date-based `YYYY.MM.N` strings. The first scheme entry is `2026.06.1`; `AppChangelog.CurrentVersion` is the release source of truth, `deploy.sh` writes that version to `build-info.json`, and the version badge opens a changelog `ActionSheet` on mobile and desktop. Future releases should update `AppChangelog.CurrentVersion` and add a matching changelog entry together.
- 2026-06-07: XNetwork version `2026.06.2` simplified the Starlink details action UI. Direct Starlink commands now use inline slide-to-confirm controls instead of the shared confirmation modal or typed `CLEAR MAP` prompt, while reset obstruction map remains a deliberate destructive action and should not be executed unless explicitly requested.
- 2026-06-07: XNetwork version `2026.06.3` polished the Starlink details sheet: duplicate local Starlink app/Diagnostics links are de-duplicated by URL, the large live summary card was collapsed into a compact status strip, and slide-to-confirm controls were made slimmer/lighter.
- 2026-06-07: XNetwork version `2026.06.4` removes the Starlink details status strip, uses Analytics-style Chart.js cards for Starlink detail graphs, maps Starlink alert field/tag `19` to `Obstruction map reset`, changes slide-to-confirm to thumb-drag-only behavior so middle-track taps do not advance commands, and hides stow/unstow controls when direct Starlink telemetry reports `has_actuators=NO`.
- 2026-06-07: XNetwork version `2026.06.5` refines the dashboard and PWA shell: Starlink action sliders have inset handles, Chart.js x-axis tick labels are hidden on app charts, redundant mode sorts live Speedify tunnel member adapters above inactive connected adapters, the connection summary no longer shows the visible traffic-breakdown hint, dashboard adapter states render as hover/focus status dots, and the PWA theme color/cache were refreshed to match the app background.
- 2026-06-07: XNetwork version `2026.06.6` visually groups dashboard adapters in redundant mode. The `Redundant group` section is populated from live Speedify tunnel rows via `IsActiveSpeedifyTunnelAdapter`, so membership follows actual tunnel usage instead of adapter names or USB positions; remaining connected/connecting adapters appear under `Other adapters`.
- 2026-06-07: Live Speedify CLI v16.8.0 does not expose a direct `currently selected redundant members` field in `show adapters` or `show settings`. In redundant mode, multiple non-proxy `stats` rows can report `connected=true` even when they are not carrying payload; infer the active redundant set from non-proxy `connection_stats` rows with live tunnel activity (`receiveBps`, `sendBps`, `totalBps`, `inFlight`, or byte deltas), capped by `maxRedundant`.
- 2026-06-08: XNetwork version `2026.06.7` corrects dashboard redundant grouping. The top group title is `Actively Redundant`, has no subtitle, uses recent non-proxy tunnel traffic and byte-counter deltas to infer active members, and caps displayed membership by `SpeedifySettings.MaxRedundant`; idle connected secondary/standby adapters remain in `Other adapters`.
- 2026-06-08: XNetwork version `2026.06.8` refines `Actively Redundant` detection to use current nonzero `receiveBps`/`sendBps`/`totalBps` tunnel throughput as the primary signal. It picks adapters carrying the dominant current traffic direction before falling back to total tunnel traffic, so connected adapters showing zero current throughput do not fill the active group. Dashboard stats rendering is batched briefly so adapter speed animations do not flicker on partial per-row stats updates.
- For new CLI response models, use `System.Text.Json` with `[JsonPropertyName]` when JSON names differ from C# names.
- Observed runtime Speedify settings have used `bondingMode: redundant`, `transportMode: udp`, `headerCompression: true`, `jumboPackets: true`, `packetAggregation: true`, and `maxRedundant: 5`; redundant mode can make Speedify tunnel throughput much higher than Cudy client throughput.
- 2026-06-05: Point-in-time runtime settings check on `xeon-network` showed `transportMode: tcp-multi`, `bondingMode: redundant`, `maxRedundant: 2`, `jumboPackets: true`, and `packetAggregation: true`; treat this as the current router setting snapshot, not an app default.
- 2026-06-05: `speedify_cli transport` supports `auto`, `tcp`, `tcp-multi`, `udp`, and `https`; there is no separate `udp-multi` mode. Multi-link behavior for UDP comes from Speedify bonding mode, adapter priority, redundant settings, and per-connection socket settings rather than a distinct UDP multi transport selector.
- 2026-06-05: User-reported field behavior: UDP transport often develops rising private-server ping over time during bad network conditions until the private Speedify connection is restarted, while `tcp-multi` has not shown the same recurring latency creep. Treat `tcp-multi` as the empirically preferred transport candidate for this router unless later tests contradict it.
- 2026-06-05: User switched back to UDP because Moonlight feels smoother on UDP. Practical interpretation: UDP is the preferred realtime/Moonlight transport when healthy, while `tcp-multi` remains the fallback for longer-term stability if UDP latency creep becomes worse than the smoothness benefit.
- Large `Estimated VPN / Overhead` in the traffic modal is not necessarily pure protocol overhead; it can include redundant-mode duplication, retransmits/loss recovery, forwarded client packets that process attribution cannot map, and timing mismatch between data sources.

## XBond Prototype
- 2026-06-13: `XBondImplementationPlan.md` defines the Rust dataplane / Blazor control-plane plan for a side-by-side reliability tunnel prototype. The key invariant is that XBond must not perform worse than the best currently working adapter because a weaker redundant adapter is present.
- 2026-06-13: The first XBond prototype lives under `xbond/` as a Rust workspace with `xbond-core`, `xbond-client`, and `xbond-server`. It is not deployed and does not route real traffic yet.
- 2026-06-13: `xbond-core` currently includes protocol framing, ChaCha20-Poly1305 sealed frame helpers, duplicate-window and deadline receiver accounting, finite JSON-safe path scoring, dynamic health-based anchor/backup role selection, and scheduler modes `AnchorOnly`, `AnchorDuplicate1`, `AnchorFec`, and `FullDuplicateDebug`.
- 2026-06-13: `xbond-client status --json --config <path>` emits XBond status for XNetwork. It uses config-derived path health until an optional runtime status file, default `/run/xbond/client-status.json`, exists; runtime status can provide observed path health and packet counters.
- 2026-06-13: `xbond-server` listens on UDP, requires a pre-shared key from the `XBOND_PSK` environment variable by default, decodes sealed frames, drops duplicates/late packets through `FrameReceiver`, and ACKs sealed heartbeat/control frames.
- 2026-06-13: XNetwork version `2026.06.9` adds read-only XBond observability at `/xbond`, desktop and mobile navigation entries, `XBondStatusService`, `XBond` config defaults, and changelog/test coverage. `XBond` is disabled by default and XNetwork does not start or stop tunnel services.
- 2026-06-13: XBond live-test preflight found `xeon-network` is `aarch64` and `xeon-speedify-vultr-01` is `x86_64`; neither host currently has `cargo` or `rustc` in PATH, so host testing requires uploaded release binaries/cross-compilation or a temporary Rust toolchain install.
- 2026-06-14: XNetwork version `2026.06.10` was committed as `15d6a99` and deployed to `xeon-network`; `/xbond` returned HTTP 200 and XBond remains disabled/not routing traffic by default.
- 2026-06-14: XBond static Linux binaries were deployed manually without systemd services: `/usr/local/bin/xbond-server` and `/usr/local/bin/xbond-client` on `xeon-speedify-vultr-01`, and `/usr/local/bin/xbond-client` on `xeon-network`.
- 2026-06-14: XBond live heartbeat test over Tailscale succeeded from `xeon-network` to `xeon-speedify-vultr-01` on `100.112.116.83:8444`: 10 sent, 10 received, 0% loss, about 55.7 ms average RTT. The server process was temporary and stopped after testing.
- 2026-06-14: Temporary public XBond heartbeat test to `45.77.241.247:8444/udp` failed 10/10 while Speedify was connected because `xeon-network` routed the server public IP through `connectify0` (`10.202.0.2`) instead of a physical WAN adapter. The temporary public UFW allow rule was removed after the test.
- 2026-06-14: Temporary Speedify streaming-bypass port rule `8444/udp` enabled a successful public XBond heartbeat test from `xeon-network` to `45.77.241.247:8444`: 10 sent, 10 received, 0% loss, about 59.1 ms average RTT. Server-side `tcpdump` on Vultr public interface `enp1s0` confirmed packets on the real public path between point-in-time client WAN IP `103.235.93.167` and `45.77.241.247:8444`. The temporary bypass rule, public UFW rule, listener, and tcpdump process were removed after the test.
- 2026-06-14: XBond lab heartbeat endpoint was installed on `xeon-speedify-vultr-01` as `xbond-server-lab.service`, listening on `0.0.0.0:8444/udp` with public UFW rule comment `XBond public heartbeat lab`. The PSK is stored only in runtime files (`/etc/xbond/server.env` and `/etc/xbond/psk` on Vultr; `/home/xeon-network/.config/XNetwork/xbond-psk` on the Pi) and is not saved in the workspace.
- 2026-06-14: XNetwork version `2026.06.12` was committed as `0a3567d` and deployed to `xeon-network`. The `/xbond` page includes an `XBond Lab` public heartbeat test button that temporarily adds Speedify streaming-bypass `8444/udp`, waits briefly, runs `xbond-client ping`, and removes the bypass afterward.
- 2026-06-14: Browser-driven verification of the XBond Lab button on `http://100.112.183.104:8080/xbond` passed: the UI reported `Passed`, 0.0% loss, about 54.4 ms average RTT, about 46.4/63.1 ms min/max RTT, and cleanup `Removed`. Follow-up checks found no lingering Speedify bypass rule for `8444` or `45.77.241.247`.
- 2026-06-14: XNetwork version `2026.06.13` was committed as `c452254` and deployed to `xeon-network`; `/xbond` returned HTTP 200. This release adds XBond multi-path shadow probing via `xbond-client multi-ping`, XNetwork per-path probe results, stricter route-warning semantics, and `xbond-server --json-events`.
- 2026-06-14: Minimal Rust 1.96 toolchains were installed for native XBond builds at `/home/xeon-network/.cargo` on `xeon-network` and `/root/.cargo` on `xeon-speedify-vultr-01`.
- 2026-06-14: `xbond-client` on `xeon-network` was rebuilt/replaced at `/usr/local/bin/xbond-client` and now supports `multi-ping`; `xbond-server` on `xeon-speedify-vultr-01` was rebuilt/replaced at `/usr/local/bin/xbond-server` and `xbond-server-lab.service` was restarted active on UDP `8444`.
- 2026-06-14: XBond lab client config is `/home/xeon-network/.config/XNetwork/xbond-client.toml`; it is disabled for routing, uses server `45.77.241.247:8444`, mode `anchor-duplicate-1`, and point-in-time bound paths for Starlink `enxc8a3627e60c1`, Smart `enx103c59f1039c`, Dito `enxb8d4bcbcb0f0`, and Globe `enxb8d4bcc3bf30`. The bind IPs can become stale when adapter IPs change.
- 2026-06-14: Direct `xbond-client multi-ping` without Speedify bypass failed 5/5 on all configured paths and correctly reported route verification failures because Linux `ip route get` still selected `connectify0`. With temporary Speedify streaming-bypass `8444/udp`, the same 4-path probe got 5/5 ACKs on all paths, with Starlink first-arrival wins and the other paths counted as duplicate drops; route verification still warns because the current verifier is not port/bypass-aware.
- 2026-06-14: XBond version `2026.06.14` was committed as `1de0685` and deployed to `xeon-network`; deployed `build-info.json` reported branch `bugfix/settings-dropdown-refresh`, commit `1de0685`, and version `2026.06.14`.
- 2026-06-14: XBond physical-path isolation was changed from source-IP binding to Linux `SO_BINDTODEVICE` by adapter interface name. The Pi lab config no longer uses `bind_addr` values, so DHCP/IP changes on the F50/Starlink adapters should not stale the XBond path definitions as long as interface names remain present.
- 2026-06-14: Direct `xbond-client multi-ping` without Speedify bypass now succeeds from `xeon-network` to public `45.77.241.247:8444/udp` using bind-device isolation across Starlink, Smart, Dito, and Globe paths. A 5-packet sample returned ACKs on all configured paths; Speedify streaming-bypass ports remained empty afterward.
- 2026-06-14: XBond traffic-engine controls are present in XNetwork but remain canary-safe: runtime config keeps `TrafficEngineMode=speedify-primary`, `AllowServiceControl=false`, `AllowPrimaryMode=false`, and `MultiPathUseSpeedifyBypass=false`. XBond primary routing cannot be selected accidentally from the UI with this config.
- 2026-06-14: `xbond-client canary-tunnel` and `xbond-server --tun-name` code paths plus systemd templates exist for real TUN canary testing, but live Pi activation was not performed because the `xeon-network` SSH user still lacks non-interactive sudo/root access for installing privileged services, capabilities, routes, NAT, and TUN ownership.
- 2026-06-14: XBond version `2026.06.15` adds canary `AnchorFec` XOR parity blocks. The client emits parity for two-packet blocks, and the server can recover one missing packet when the paired data packet plus parity arrive. This is code/test coverage only until a privileged TUN canary can be activated.
- 2026-06-14: XBond version `2026.06.15` was committed as `4701849`, pushed to `bugfix/settings-dropdown-refresh`, deployed to `xeon-network`, and rebuilt into `/usr/local/bin/xbond-client` on the Pi and `/usr/local/bin/xbond-server` on `xeon-speedify-vultr-01`. Post-deploy checks showed XNetwork `/xbond` HTTP 200, `xbond-server-lab.service` active on UDP `8444`, and no Speedify streaming-bypass ports. A point-in-time 3-packet physical multi-path probe saw ACKs on Smart/Dito/Globe and 0/3 ACKs on Starlink.
- 2026-06-14: `xeon-network` user `xeon-network` now has passwordless sudo (`sudo -n true` succeeds). Direct `root@xeon-network` SSH remained denied when checked, but passwordless sudo is enough for privileged XBond service and route work.
- 2026-06-14: XBond version `2026.06.16` was committed as `3e753e7`, pushed, deployed, and rebuilt on both live hosts. It makes the canary TUN bidirectional: the Pi client writes return packets to `xbond0`, and the Vultr server reads `xbonds0` return packets and sends them back to known client path peers.
- 2026-06-14: XBond canary services were installed with runtime PSK env files outside the repo: Pi `/etc/systemd/system/xbond-client.service` using `/etc/xbond/client.toml` and `/etc/xbond/client.env`; Vultr `/etc/systemd/system/xbond-server-canary.service` using `/etc/xbond/server.env`. Both services were active but disabled for boot when checked.
- 2026-06-14: XBond canary TUN addressing used Pi `xbond0` = `10.250.0.2/30` and Vultr `xbonds0` = `10.250.0.1/30`. `ping -I xbond0 10.250.0.1` passed 5/5 with about 59.5 ms average RTT.
- 2026-06-14: A scoped internet canary over XBond succeeded without changing the default route: runtime server forwarding/NAT was added for `10.250.0.0/30`, a temporary Pi route `1.1.1.1/32 dev xbond0 src 10.250.0.2` was added for the test, `ping -I xbond0 1.1.1.1` passed 5/5 with about 77.6 ms average RTT, and the temporary client route was removed afterward. After removal, `ip route get 1.1.1.1` returned to `connectify0`.
- 2026-06-14: XBond canary restart regression was fixed in commits `b55fd2a`, `0e14fc6`, and `cbf8e39`: `xbond-client canary-tunnel` now uses a fresh runtime session id by default, canary systemd units reapply `10.250.0.2/30` on `xbond0` and `10.250.0.1/30` on `xbonds0` after each start, and long-running client/server tunnel loops log per-path UDP send failures instead of exiting.
- 2026-06-14: After the restart-regression fixes were deployed and binaries rebuilt on both hosts, `ping -I xbond0 10.250.0.1` passed 5/5 with about 64.9 ms average RTT. A scoped `1.1.1.1/32` route over XBond then passed 10/10 with about 70.2 ms average RTT, `xbond-client.service` restart count stayed unchanged, and the temporary client route was removed so `1.1.1.1` returned to `connectify0`.
- 2026-06-14: XNetwork version `2026.06.17` was committed as `d863d07`, pushed, and deployed to `xeon-network`. This release enables boot-safe XBond service controls in `/xbond`, shows the client service boot state, and adds guarded scoped `/32` route canary controls for testing one IPv4 host through `xbond0` while Speedify remains the default route.
- 2026-06-14: XBond services are now enabled at boot. On `xeon-network`, `xbond-client.service` is active/enabled with `xbond0` at `10.250.0.2/30`; on `xeon-speedify-vultr-01`, `xbond-server-canary.service` is active/enabled with `xbonds0` at `10.250.0.1/30`.
- 2026-06-14: Persistent Vultr XBond forwarding/NAT is installed as `xbond-server-nat.service` and active/enabled. It runs `/usr/local/sbin/xbond-server-nat-apply` and rolls back with `/usr/local/sbin/xbond-server-nat-rollback`, forwarding/masquerading `10.250.0.0/30` through public interface `enp1s0`. The Pi rollback helper is `/usr/local/sbin/xbond-client-rollback`.
- 2026-06-14: Post-boot-enable verification passed: XNetwork `/xbond` returned HTTP 200 and showed version `2026.06.17`; `ping -I xbond0 10.250.0.1` passed 5/5 with about 58.7 ms average RTT; a temporary scoped `1.1.1.1/32 dev xbond0 src 10.250.0.2` route passed 10/10 with about 58.8 ms average RTT, then was removed so `ip route get 1.1.1.1` returned to Speedify `connectify0`.
- 2026-06-14: The older `xbond-server-lab.service` is intentionally disabled because it binds the same `0.0.0.0:8444/udp` port as `xbond-server-canary.service`; leaving both enabled would create a boot-time port race. The canary server still answers heartbeat traffic on `8444`, including a 3/3 Tailscale heartbeat test to `100.112.116.83:8444` with about 64.0 ms average RTT.
- 2026-06-14: Live `xbond-client multi-ping --config /etc/xbond/client.toml --count 5` from `xeon-network` to public `45.77.241.247:8444` verified device-bound multipath delivery while the default route still pointed at Speedify `connectify0`: Smart, Dito, and Globe each returned 5/5 ACKs with 0% loss, while Starlink `enxc8a3627e60c1` was `NO-CARRIER` and returned 0/5. The current `anchor-duplicate-1` probe still selected path 1/Starlink as anchor, so dynamic anchor demotion for down or 100%-loss paths remains a needed improvement before production use.
- 2026-06-15: Follow-up fixes on `feature/xband-only-runtime` hard-exclude down/NO-CARRIER, cooldown, repeated-send-failure, socket-missing, and full-loss paths from anchor/backup role assignment. If every path is bad, XBond reports no anchor instead of choosing the least bad path.
- 2026-06-15: XBond defaults on `feature/xband-only-runtime` are now two-link duplicate mode: `anchor-duplicate-1` with one active backup. `AnchorFec` remains supported as an explicit mode but is not the default.
- 2026-06-15: XNetwork now reads `/run/xbond/client-status.json` directly through `XBondStatusService` and computes roles/schedule in-process for dashboard and analytics, instead of spawning `xbond-client status` for every UI refresh.
- 2026-06-15: XBond-only branch commits `c0177de` (`Create XBond-only runtime branch`) and `f0bef87` (`Fix XBond-only runtime scheduling gaps`) were pushed to `origin/feature/xband-only-runtime`.
- 2026-06-15: `feature/xband-only-runtime` commit `f0bef87` was deployed to `xeon-network`; deployed `build-info.json` reported version `xbond-2026.06.18`, branch `feature/xband-only-runtime`, and commit `f0bef87`. Local app routes `/`, `/details`, `/xbond`, `/xrouter`, and `/settings` returned HTTP 200.
- 2026-06-15: On `xeon-network`, `xbond-client.service` is active/enabled and runs `/usr/local/bin/xbond-client tunnel --config /etc/xbond/client.toml --tun-name xbond0 --tun-mtu 1400 --json-events`. Runtime config is `/etc/xbond/client.toml`, runtime status is `/run/xbond/client-status.json`, and `xbond0` uses `10.250.0.2/30`.
- 2026-06-15: During post-deploy verification, XBond runtime status showed mode `anchor-duplicate-1`, server `45.77.241.247:8444`, Smart `enx103c59f1039c` as anchor, Dito `enxb8d4bcbcb0f0` as duplicate, Globe `enxb8d4bcc3bf30` as probe, and Starlink `enxc8a3627e60c1` unavailable because the interface was down/NO-CARRIER.
- 2026-06-15: On `xeon-speedify-vultr-01`, `xbond-server.service` and `xbond-server-nat.service` are active/enabled, `xbond-server-canary.service` is inactive/disabled, `xbond-server` listens on UDP `0.0.0.0:8444`, and `xbonds0` uses `10.250.0.1/30`.
- 2026-06-15: XBond live checks passed after the XBond-only deployment: `ping -I xbond0 10.250.0.1` passed 5/5 with about 59.6 ms average RTT, and a temporary scoped `1.1.1.1/32 dev xbond0 src 10.250.0.2` route passed 10/10 with about 60.3 ms average RTT. The temporary `/32` route was removed afterward; the Pi default route was not permanently replaced with `xbond0`.
- 2026-06-15: An initial bounded production-route cutover test stopped `speedify.service`, installed a temporary IPv4 default route through `xbond0`, verified `ip route get 1.1.1.1` used `xbond0`, and pinged `1.1.1.1` 10/10 with about 67.4 ms average RTT. That test restored Speedify afterward, but this was corrected the same day because `feature/xband-only-runtime` must not run Speedify.
- 2026-06-15: XBond-only runtime correction stopped and disabled `speedify.service` and `speedify-sharing.service` on `xeon-network`; both verified inactive/disabled. `xbond-client.service` is active/enabled and now owns the IPv4 default route through `xbond0`.
- 2026-06-15: `xbond-client.service` now conflicts with Speedify services and runs `/usr/local/sbin/xbond-client-route-apply` after startup. That helper pins the XBond server IPv4 endpoint `45.77.241.247/32` to the best physical default route before installing `default dev xbond0 src 10.250.0.2 metric 1`, preventing the tunnel server endpoint from routing through the tunnel itself.
- 2026-06-15: XBond realtime deadline was raised from 120 ms to 500 ms for reliability-first testing. With Speedify stopped/disabled, route `45.77.241.247` went via physical Smart `enx103c59f1039c`, route `1.1.1.1` went via `xbond0`, `ping -I xbond0 10.250.0.1` passed 30/30 with 0% loss, and `ping 1.1.1.1` passed 30/30 with 0% loss.
- 2026-06-15: Live `xnetwork.service` on `xeon-network` was cleaned up for the XBond-only branch: the unit description is now `XNetwork Dashboard (Blazor Server)` and the stale `speedify_cli` comment was removed. The service remained active after `systemctl daemon-reload`.
- 2026-06-15: `xbond-client tunnel` now sends periodic per-path heartbeat probes and writes live `rtt_ms`, `jitter_ms`, and `loss_rate` into `/run/xbond/client-status.json`; the rebuilt Pi binary was installed at `/usr/local/bin/xbond-client` and `xbond-client.service` was restarted active/enabled.
- 2026-06-15: XNetwork `InterfaceMetadataService` now prefers live adapter display names from NetworkManager and then F50/ZTE modem gateway APIs. Active F50 gateways `192.168.3.1` and `192.168.4.1` answered `goform_get_cmd_process` with `network_provider` values `SMART` and `DITO` when called with local `Referer`/`Origin` headers. Generic NetworkManager names such as `Wired connection 3` are ignored.
- 2026-06-15: XNetwork code commits `38310bf` and `eca6031` fixed XBond path telemetry and live modem provider names on `feature/xband-only-runtime`; a later docs-only deploy refreshed `build-info.json`. Headless Chrome verification of `http://xeon-network:8080/` and `/details` showed live provider labels `SMART`/`DITO`, active-path RTT/loss, adaptive kbps chart labels, and nonzero analytics RTT values.
- 2026-06-15: XNetwork version `xbond-2026.06.28` fixes Performance Matrix artifact writes and native iperf reporting. `/var/lib/xnetwork/diagnostics` should exist on `xeon-network` and be writable by `xeon-network`; if not, `XBondSpeedTestService` falls back to user-local diagnostics storage. Native per-adapter iperf tests require a deliberately exposed public/native iperf3 listener (default config `45.77.241.247:5202`); the deployed `xbond-iperf3.service` is tunnel-only at `10.250.0.1:5201` and is not a native-adapter test endpoint.
- 2026-06-15: `XBondPerformanceOptimizationPlan.md` records the current performance-improvement plan. Deep diagnostics such as heartbeat, multi-path, bad-backup simulation, Performance Matrix, MTU sweep, and scoped route tests are engineering/operator tooling; the user-facing `/xbond` page should expose only the simple tunnel speed test plus runtime/path status.
- 2026-06-15: XNetwork version `xbond-2026.06.29` keeps the same Performance Matrix behavior but compacts native iperf JSON failures into concise user-facing error text.
- 2026-06-15: XNetwork version `xbond-2026.06.31` makes `/xbond` read-only except for the manual tunnel speed test; service start/stop/boot controls and deep diagnostics are not user-facing on that page.
- 2026-06-16: XNetwork version `xbond-2026.06.32` adds live XBond diagnostic overrides through local Unix socket `/run/xbond/client-control.sock`. Operator tooling can run `xbond-client override set --mode <mode> --policy diagnostic --ttl-seconds <n>` and `xbond-client override clear` without rewriting `/etc/xbond/client.toml` or restarting `xbond-client.service`.
- 2026-06-16: XBond role selection now uses scheduler hysteresis: a better anchor must beat the current anchor by the configured margin for multiple scheduler ticks before replacing it, while hard-demoted paths such as down interfaces, repeated send failures, stale ACKs, full loss, or saturated queues are replaced immediately.
- 2026-06-16: XBond client/server hot-path packet sends use borrowed payload encoding before encryption to avoid an extra packet clone per selected path. Protocol/runtime changes still require paired deployment of `xbond-client` on `xeon-network` and `xbond-server` on `xeon-speedify-vultr-01`.
- 2026-06-16: XNetwork version `xbond-2026.06.33` adds `/wifi` as an alias for the existing `/xrouter` Wifi page; keep `/xrouter` for compatibility.
- 2026-06-16: XNetwork version `xbond-2026.06.34` makes backend XBond diagnostic overrides use `sudo -n xbond-client override ...` when service-control sudo is configured. The control socket remains root-owned; do not make `/run/xbond/client-control.sock` world-writable just so app diagnostics can use it.
- 2026-06-16: XNetwork version `xbond-2026.06.35` adds `deploy-xbond-paired.ps1` for paired app/client/server deployment, removes persisted `Diagnostic` from normal Settings policy choices, hides scoped IPv4 route diagnostics from Settings, and uses an absolute `/usr/local/bin/xbond-client` path for sudo-backed diagnostic override commands.
- 2026-06-16: XNetwork version `xbond-2026.06.36` aligns hidden XBond route/scope defaults with the dashboard latency probe by using Google DNS `8.8.8.8` instead of `1.1.1.1`; `/xbond` remains limited to runtime/path status plus the simple tunnel speed test.
- 2026-06-16: XBond Rust performance audit found the main remaining dataplane costs are packet-copy/allocation pressure in TUN/channel/protocol paths, unbounded hot `mpsc` packet queues, sequential send/encrypt work in the main client/server loops, server-side payload clones for FEC/reorder, per-packet crypto context construction, `BTreeMap`/`Vec` reorder allocations, per-packet scheduler `Vec` construction, and synchronous pretty JSON status writes. Prioritize low-risk quick wins first: cache the AEAD cipher, compact/offload status writes, precompute small transmission plans, bound queues with explicit drop counters, and then tackle buffer pooling/in-place AEAD/per-path sender tasks.
- 2026-06-16: XBond Rust dataplane Phase 1 implements low-risk runtime performance improvements while preserving wire compatibility: cached `XChaCha20Poly1305` cipher construction, compact runtime status JSON, precomputed small/bulk transmission plans, bounded TUN/inbound packet queues, UDP socket buffer requests, and runtime telemetry for queue depth/skips. This touches both `xbond-client` and `xbond-server`, so paired deployment remains mandatory.
- 2026-06-16: `deploy-xbond-paired.ps1` wraps remote multi-line commands through base64 before piping to remote bash, sources the remote Cargo environment explicitly, and installs the `xbond-server.service` template before daemon reload. This avoids stale-binary deployments caused by SSH argument splitting around `bash -lc`.
- 2026-06-16: XBond Phase 1 runtime commit `5809131` was paired-deployed to `xeon-speedify-vultr-01` and `xeon-network`. Verification showed compact `/run/xbond/client-status.json`, queue/socket telemetry fields present, `xbond-server.service` using explicit queue/socket flags, app routes `/`, `/xbond`, `/details`, `/settings`, `/xrouter`, and `/wifi` returning HTTP 200, default route to `8.8.8.8` via `xbond0`, and 5/5 pings to both `10.250.0.1` and `8.8.8.8`.
- 2026-06-16: Post-Phase-1 bounded tunnel tests from `xeon-network` measured about 172 Mbps upload sender / 167 Mbps receiver to `10.250.0.1:5201`, about 150 Mbps reverse sender / 123 Mbps receiver, and public `speedtest-cli` through XBond at about 93 Mbps down / 70 Mbps up / 121 ms ping. After the tests, runtime telemetry showed RSS about 6.9 MB, TUN/inbound queue depths 0, queue drops 0, UDP socket buffer request 4 MiB, and duplicate send skips counted under load.
- 2026-06-16: XBond Rust dataplane Phase 2/3 adds single-buffer sealed-frame encoding, no-copy sealed-frame header parsing before decrypt, detached in-place AEAD helpers, server-side reduction of ingress payload clones when FEC is inactive, and per-path bounded sender workers for client outbound and server return traffic. This preserves wire compatibility but changes both `xbond-client` and `xbond-server`, so paired deployment remains mandatory.
- 2026-06-16: During Phase 2/3 deployment, `xbond-server.service` could receive packets before `ExecStartPost` finished bringing `xbonds0` fully up, causing a transient TUN write `Input/output error`; client and server TUN write paths now log/drop that packet instead of crashing the process.
- 2026-06-16: XBond Phase 2/3 runtime commit `722d6f6` was paired-deployed to `xeon-speedify-vultr-01` and `xeon-network`. Verification showed `xbond-server.service`, `xbond-server-nat.service`, `xnetwork.service`, and `xbond-client.service` active; default route to `8.8.8.8` via `xbond0`; 5/5 pings to `10.250.0.1` averaging about 37 ms; 5/5 pings to `8.8.8.8` averaging about 38 ms; tunnel iperf upload about 186 Mbps sender / 183 Mbps receiver; tunnel iperf reverse/download about 150 Mbps sender / 135 Mbps receiver after retry; public speedtest about 61.9 Mbps down / 119.1 Mbps up / 72.6 ms.

## Probe And Auto Server Switching
- `SpeedifyProbeAgent` is a separate minimal API that scores Speedify servers from an external vantage point; deployed probe service has been `speedify-probe-agent` on VM `speedify-probe` with Tailscale IP `100.114.215.110` and API base `http://100.114.215.110:8090`.
- Do not write the probe API key into repo files; it belongs in runtime config only.
- Router `AutoServerSwitchService` uses `ProbeScoreClient`, `ServerSwitchRecommendationSelector`, `LocalWanStabilityEvaluator`, `RecommendationConfidenceTracker`, and `AutoServerSwitchStateStore`.
- Auto-switch can block switching when the probe says the current server is still acceptable, treating local router health as a WAN issue.
- Router Tailscale connectivity can be affected by Speedify routing; restarting Speedify has previously restored router-to-probe connectivity.
- `PrivateReconnectService` is a hosted service for refreshing the private Speedify session with `speedify_cli disconnect`, a short delay, then `speedify_cli connect private`; it is disabled by default and configured by `PrivateReconnect` / `private-reconnect-settings.json`.
- Private reconnect defaults: `Enabled=false`, `IntervalMinutes=30`, `DelaySeconds=2`; the Settings page can enable it, change the interval/delay, view status/events, and trigger `Reconnect Private Now`.
- 2026-06-04: A scheduling bug was fixed in `PrivateReconnectService`: do not reschedule `NextAttemptUtc` after every sleep/check tick, or the due time is pushed forward forever and only manual reconnect works.
- Private reconnect also supports an optional high-latency trigger: only while connected to a verified private Speedify server, sustained latency defaults to `>=300ms` for `120s`, then it reconnects, observes recovery for `60s`, and if latency remains high it suppresses further health-triggered reconnects for `15m`. Manual reconnect and the scheduled timer remain separate.

## Linux And Privileges
- `NetworkMonitorService` exits unless the app runs on Linux, monitoring is enabled, and `NetworkMonitor:WhitelistedLinks` is non-empty.
- `WhitelistedLinks` is compared to `Adapter.Name` from `show adapters` (interface-like names such as `enxc8...`), not ISP/provider text.
- The monitor checks every 1s and restarts down whitelisted links after `DownTimeoutSeconds` via `/bin/bash -c "ip link set <iface> down/up"`; this needs root/process privileges.
- OS routing priority parses Linux `ip route`/`ip addr` output and applies changes with `sudo ip route ...`; web requests cannot answer sudo prompts.
- Linux system controls in `Settings.razor` call `sudo systemctl restart speedify || sudo service speedify restart` and `sudo /sbin/reboot`; configure NOPASSWD using `LINUX_SETUP.md` before testing those buttons.
- `LocalProcessTrafficService` uses `nethogs` for live process throughput on `connectify0`; `nethogs` needs root or capabilities. Use `sudo setcap cap_net_admin,cap_net_raw+eip /usr/sbin/nethogs` if process attribution is unavailable.

## Blazor Runtime
- Components are server-side interactive from `App.razor`; timer, stream, and background callbacks should marshal UI updates with `InvokeAsync(StateHasChanged)`.
- Blazor reconnect is customized in `App.razor` with `components-reconnect-modal`, `wwwroot/js/blazorReconnect.js`, and reconnect CSS in `wwwroot/app.css`; the UI should stay a minimalist bottom-center toast with a spinner plus one status line, no action buttons, and a blurred background that transitions in/out. Rejected/expired circuits auto-reload after a short delay using a MutationObserver on the reconnect element class, not only the Blazor state-change event, because mobile/PWA sessions have shown the rejected class without the event handler triggering reload. Blazor autostart is disabled so the script can set 15s server timeout, 5s keepalive, fast early retry intervals, and background retries after built-in retry failure.
- Server-side SignalR circuit timings are also tuned in `Program.cs` with `ClientTimeoutInterval=15s`, `HandshakeTimeout=15s`, and `KeepAliveInterval=5s`.
- `Home.razor` runs a 3s adapter refresh, 10s server refresh, one Speedify stats stream, and a traffic breakdown timer only while the modal is open.
- `ConnectionHealthService` pings `8.8.8.8` every 500ms; avoid adding unbounded extra health probe consumers.
- `Statistics.razor` is the `/details` page; chart setup intentionally takes two render cycles using `_readyForChartInitializationStep` before initializing canvases and starting streaming.
- `ConnectionSummary.razor` is the dashboard throughput card; it opens the Traffic Breakdown modal when XRouter/Cudy config is available.
- JS interop can throw `JSDisconnectedException` during live chart updates or disposal; stop timers/streams or ignore it during disconnect cleanup.
- Mobile is first-class; avoid regressions in bottom tab bar, modal z-index, safe-area padding, and sticky headers.
- 2026-06-07: SpeedifyUi/XNetwork Starlink telemetry and health-based adapter sorting were deployed to `xeon-network` as app commit `82bc533` on branch `bugfix/settings-dropdown-refresh`; post-deploy checks showed `xnetwork.service` active, HTTP 200 locally and over Tailscale, and rendered dashboard Version `v22` displaying a Starlink row with direct dish telemetry.

## Traffic Breakdown
- Dashboard `Traffic Breakdown` is opened by tapping/clicking the main throughput card; the old always-visible inline attribution card was removed.
- Modal refreshes every 1s while open and shows Speedify Tunnel, Wifi Clients, Local Processes, and Estimated VPN / Overhead.
- Wifi client totals come from Cudy device table via `XRouterService.GetClientsAsync`; cache duration is 750ms so 1s UI refreshes do not over-fetch within a tick.
- Local process totals come from `LocalProcessTrafficService`, which runs `nethogs -t -b -C -d 1 -c 1 connectify0` and parses KB/s into Mbps.
- Zero-throughput nethogs rows are filtered. Non-zero `unknown TCP/0/0` or `unknown UDP/0/0` rows are shown as `Unknown tunnel flow` because the kernel/nethogs cannot map those packets to a Pi process.
- Estimated VPN / Overhead is `Speedify Tunnel - Wifi Clients - Local Processes`, clamped at zero. Treat it as an estimate, not an exact byte-accounting ledger.

## Wifi And Cudy Client Management
- The UI nav label is `Wifi`, but the route is still `/xrouter` and the component is `XRouter.razor`.
- `XRouter.razor` lists Cudy clients, shows hostname/IP/MAC/connection type/signal/online duration, live upload/download throughput, per-device history, block/unblock, and per-device rate limits.
- Cudy client list refreshes every 1s from the Cudy client table.
- Known Cudy client endpoints are `/cgi-bin/luci/admin/network/devices/devlist?detail=1`, `/cgi-bin/luci/admin/network/devices/internet`, and `/cgi-bin/luci/admin/network/devices/devinfo`.
- Cudy data caps were not implemented because the inspected firmware endpoint did not expose them.
- `CudyXRouterClientParser` parses the Cudy HTML table; keep parser tests updated when endpoint markup changes.

## Cudy AP Automation
- Cudy TR3000 control is via LuCI web forms and service apply, not physical power control.
- Runtime Cudy management has used private HTTPS at `https://192.168.145.231/`; HTTP port 80 was refused. Self-signed certificate validation is allowed only for private HTTPS management IPs.
- Login quirk: `/cgi-bin/luci` serves the login form while `/cgi-bin/luci/` can return `403`.
- Cudy wireless form paths include `/cgi-bin/luci/admin/network/wireless/config/combo`, `/cgi-bin/luci/admin/network/wireless/config/uncombine`, and `/cgi-bin/luci/admin/network/wireless/config/combine`.
- Cudy disabled fields are `cbid.wireless.wlan00.disabled` for 2.4 GHz, `cbid.wireless.wlan10.disabled` for 5 GHz, and `cbid.wireless.wlan.disabled` for Smart Connect/combined mode; value `1` means disabled.
- Posting the wireless form only saves config; the app must also POST `/cgi-bin/luci/admin/servicectl/restart/wireless,vlan` and poll `/cgi-bin/luci/admin/servicectl/status` until `finish`.
- Cudy admin password is persisted separately from non-secret automation settings. Secret file is `cudy-admin-password`; non-secret JSON is `cudy-ap-automation-settings.json`.
- Runtime Linux paths have been `/home/xeon-network/.config/XNetwork/cudy-admin-password` and `/home/xeon-network/.config/XNetwork/cudy-ap-automation-settings.json`; do not commit either.
- Cudy AP SSIDs `A` and `A.2g` disappeared from Pi scan after service apply during prior verification.

## Router Wi-Fi Tooling
- `WifiService` uses Linux `nmcli` for Wi-Fi scan/status/connect, default interface `wlan0`; password input is passed through stdin and cleared after submit.
- Router tooling observed: `nmcli` at `/usr/bin/nmcli`, `wpa_cli` at `/usr/sbin/wpa_cli`, interface `wlan0`.
- Home Wi-Fi automation stores both SSID and BSSID; Cudy automation can disable Cudy AP bands when the home network is present with sufficient signal and re-enable when missing/weak.
- `CudyApAutomationSettings` runtime defaults include `Disable2G: true`, `Disable5G: true`, signal thresholds, and check timing; verify actual runtime config before changing behavior.

## Settings Page
- `Settings.razor` is a large accordion/modal page; reusable controls live in `XNetwork/Components/Custom` and related styles are in `XNetwork/wwwroot/app.css`.
- Most settings apply immediately through CLI handlers; pending flags `_hasUnsavedBypassChanges`, `_hasUnsavedStreamingChanges`, and `_adapterPendingChanges` only cover selected rule/list/adapter edits.
- The `Private Server Reconnect` Settings accordion persists via `PrivateReconnectSettingsStore` and triggers manual/scheduled reconnects through `PrivateReconnectService`; do not call `speedify_cli` directly from UI code.
- Streaming and gaming service presets are hardcoded dictionaries, while `_enabledServices` is a separate case-insensitive `HashSet` that must stay in sync with `_bypassSettings.Services`.
- Adapter settings cache local state in `_adapterLocalState`; UI converts MB/GB to bytes for data limits, displays adapter rate limits in Mbps while converting Speedify's returned bytes/sec to command bits/sec on save, and only offers monthly reset days 1-28.
- Adapter encryption state is not returned by `speedify_cli show adapters`; read it from `speedify_cli show settings` using `perConnectionEncryptionEnabled` / `perConnectionEncryptionSettings` and merge it into adapter rows. Missing per-adapter entries inherit the global `encrypted` setting.
- Port rules for streaming, bypass, and fixed-delay are formatted in `SpeedifyService.FormatPortRule` as `port[-end]/protocol`.

## Tests
- Test project is `XNetwork.Tests`; current tests cover Cudy form/client parsing, local process traffic parsing, rolling health windows, probe score calculations, server switch recommendations, recommendation confidence, auto-switch state store, and local WAN stability.
- Latest local full test run during the adapter rate-limit unit fix passed 34 tests; do not hard-code that as a permanent invariant.
- Add focused tests for parser changes because Cudy/LuCI endpoints are HTML/form based and firmware-specific.

## Instruction Sources
- Treat this root `AGENTS.md` as current; `CLAUDE.md` is older and broader.
- `.roo/rules-*` files contain useful historical gotchas but also stale claims, so verify them against code before preserving anything.
