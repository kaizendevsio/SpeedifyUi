@page "/"
@using Microsoft.JSInterop
@using XNetwork.Components.Custom
@using XNetwork.Models
@using XNetwork.Services
@using XNetwork.Utils
@implements IAsyncDisposable
@inject XBondSnapshotCache XBondSnapshotCache
@inject IStarlinkTelemetryService StarlinkTelemetryService
@inject StarlinkTelemetrySettings StarlinkTelemetrySettings
@inject LocalDeviceProxyService LocalDeviceProxyService
@inject NavigationManager NavigationManager
@inject IJSRuntime JSRuntime

<PageTitle>Dashboard - XNetwork</PageTitle>

<div class="p-4 md:p-6">
    <div class="hidden md:block">
        <h2 class="mb-1 text-3xl font-bold text-white">Dashboard</h2>
        <p class="mb-6 text-slate-400">Manage and monitor network adapters in real-time.</p>
    </div>

    @if (_snapshot is null)
    {
        <div class="space-y-6" aria-label="Loading dashboard">
            <div class="rounded-xl border border-slate-700 bg-slate-800/50 p-4 shadow-lg">
                <div class="flex items-center gap-3">
                    <SkeletonBlock CssClass="w-10 h-10 rounded-full flex-shrink-0" />
                    <div class="flex-grow space-y-2">
                        <SkeletonBlock CssClass="h-5 w-2/5" />
                        <SkeletonBlock CssClass="h-4 w-3/5" />
                    </div>
                </div>
            </div>

            <div class="rounded-xl border border-slate-700 bg-slate-800/50 p-5 shadow-lg">
                <SkeletonBlock CssClass="h-5 w-1/3 mb-5" />
                <div class="grid grid-cols-3 gap-4">
                    @for (var i = 0; i < 3; i++)
                    {
                        <div class="space-y-3 rounded-lg bg-slate-900/50 p-4">
                            <SkeletonBlock CssClass="h-4 w-3/5" />
                            <SkeletonBlock CssClass="h-8 w-2/3" />
                        </div>
                    }
                </div>
            </div>

            <div class="space-y-3">
                <div class="flex items-center gap-3 px-1">
                    <SkeletonBlock CssClass="h-6 w-28" />
                    <SkeletonBlock CssClass="h-7 w-20 rounded-full" />
                </div>
                @for (var i = 0; i < 3; i++)
                {
                    <div class="rounded-xl border border-slate-700 bg-slate-800/50 px-3 py-3 shadow-lg">
                        <div class="flex items-center justify-between gap-3">
                            <div class="flex min-w-0 flex-grow items-center gap-3">
                                <SkeletonBlock CssClass="w-10 h-6" />
                                <div class="flex-grow space-y-2">
                                    <SkeletonBlock CssClass="h-5 w-2/5" />
                                    <SkeletonBlock CssClass="h-4 w-3/5" />
                                </div>
                            </div>
                            <SkeletonBlock CssClass="h-7 w-7 rounded-full" />
                        </div>
                        <div class="mt-3 flex items-center gap-4">
                            <SkeletonBlock CssClass="h-4 w-20" />
                            <SkeletonBlock CssClass="h-4 w-20" />
                            <SkeletonBlock CssClass="h-4 w-16" />
                        </div>
                    </div>
                }
            </div>
        </div>
    }
    else
    {
        @if (_snapshot.HasError && !string.IsNullOrWhiteSpace(_snapshot.Error))
        {
            <div class="mb-6 rounded-xl border border-red-500/35 bg-red-500/10 p-4 text-red-100" role="alert">
                <div class="flex items-start gap-3">
                    <i class="fas fa-terminal mt-0.5 text-red-300"></i>
                    <div class="min-w-0">
                        <p class="font-semibold text-white">XBond status</p>
                        <p class="mt-1 text-sm text-red-100">@_snapshot.Error</p>
                    </div>
                </div>
            </div>
        }

        <div class="mb-6 rounded-xl border border-slate-700 bg-slate-800/50 p-4 shadow-lg">
            <div class="flex items-center gap-3">
                <div class="@GetServerIconClass()">
                    <i class="fas fa-server text-lg"></i>
                </div>
                <div class="min-w-0 flex-grow">
                    <div class="mb-1 flex items-center gap-2">
                        <h3 class="truncate text-base font-semibold text-white">XBond Tunnel</h3>
                        <span class="@GetServerBadgeClass()">@StateLabel</span>
                    </div>
                    <div class="flex flex-wrap items-center gap-x-3 gap-y-1 text-sm text-slate-400">
                        <span class="inline-flex items-center gap-1.5 text-slate-300" title="XBond redundancy policy">
                            <i class="fas fa-layer-group text-xs text-emerald-300"></i>
                            <span>@FormatMode(_snapshot.RedundancyPolicy)</span>
                        </span>
                        @if (_snapshot.IsProtectingFromLoss)
                        {
                            <span class="inline-flex items-center gap-1 rounded-full border border-cyan-400/35 bg-cyan-500/10 px-2 py-0.5 text-xs font-medium text-cyan-100" title="@_snapshot.ProtectionReason">
                                <i class="fas fa-shield-alt text-[0.65rem]"></i>
                                <span>Protecting from loss</span>
                            </span>
                        }
                        @if (_snapshot.IsRecoveryActive)
                        {
                            <span class="inline-flex items-center gap-1.5 rounded-full border border-amber-500/30 bg-amber-500/10 px-2 py-0.5 text-xs font-semibold text-amber-100" title="@_snapshot.RecoveryReason">
                                <i class="fas fa-shield-heart text-[0.65rem]"></i>
                                <span>Recovery @_snapshot.RecoveryHoldMs ms</span>
                            </span>
                        }
                    </div>
                </div>
            </div>
        </div>

        <ConnectionSummary
            ConnectionStatus="@ConnectionTitle"
            Latency="@GetLatencyValue()"
            Download="@_snapshot.DownloadMbps"
            Upload="@_snapshot.UploadMbps"
            AnchorDownload="@GetAnchorDownloadMbps()"
            BackupDownload="@GetBackupDownloadMbps()"
            IsStable="@IsConnectionStable()"
            IsRecoveryActive="@(_snapshot?.IsRecoveryActive == true)"
            RecoveryHoldMs="@(_snapshot?.RecoveryHoldMs ?? 0)"
            RecoveryReason="@(_snapshot?.RecoveryReason ?? string.Empty)"
            ShowTrafficDetailsHint="false" />

        <section class="space-y-3">
            <div class="flex items-center gap-3 px-1">
                <h3 class="text-lg font-semibold text-white">Adapters</h3>
                <span class="rounded-full bg-green-500/10 px-3 py-1 text-sm font-medium text-green-400">
                    @GetLivePathCount() live
                </span>
            </div>

            @if (_snapshot?.DashboardPaths.Any() != true)
            {
                <div class="rounded-xl border border-slate-700 bg-slate-800/50 p-5 text-slate-400 shadow-lg">
                    No XBond adapters found.
                </div>
            }
            else
            {
                <div class="space-y-4" @ref="_adapterListElement">
                    @if (_snapshot.ActivePaths.Count > 0)
                    {
                        <section class="rounded-xl border border-cyan-500/25 bg-cyan-500/[0.06] p-3">
                            <div class="mb-3 flex items-center gap-2 px-1">
                                <span class="inline-flex h-6 w-6 items-center justify-center rounded-lg bg-cyan-500/10 text-cyan-300">
                                    <i class="fas fa-shield-halved text-xs"></i>
                                </span>
                                <h4 class="text-base font-semibold text-white">Actively Redundant</h4>
                                <span class="inline-flex min-w-6 items-center justify-center rounded-full border border-cyan-400/30 bg-cyan-400/10 px-2 py-0.5 text-xs font-semibold text-cyan-100">
                                    @_snapshot.ActivePaths.Count
                                </span>
                            </div>
                            <div class="space-y-3">
                                @foreach (var path in _snapshot.ActivePaths)
                                {
                                    @PathCard(path)
                                }
                            </div>
                        </section>
                    }

                    @if (_snapshot.StandbyPaths.Count > 0)
                    {
                        <div class="space-y-3">
                            @foreach (var path in _snapshot.StandbyPaths)
                            {
                                @PathCard(path)
                            }
                        </div>
                    }
                </div>
            }
        </section>
    }
</div>

<ActionSheet IsOpen="_showStarlinkDetails"
             Title="@GetStarlinkSheetTitle()"
             Description="@GetStarlinkSheetDescription()"
             CloseLabel="Close Starlink details"
             MaxWidthClass="md:max-w-5xl"
             OnClose="CloseStarlinkDetailsAsync">
    @{
        var starlinkSnapshot = StarlinkTelemetryService.GetSnapshot();
        var starlinkHistory = StarlinkTelemetryService.GetHistory();
        var starlinkCapabilities = StarlinkTelemetryService.GetCapabilities();
        var detectedCapabilities = starlinkCapabilities.Capabilities.Where(capability => capability.IsDetected).ToList();
        var openCapabilities = GetUniqueStarlinkOpenCapabilities(detectedCapabilities);
        var commandCapabilities = detectedCapabilities
            .Where(capability => !string.IsNullOrWhiteSpace(capability.DirectCommand) && IsStarlinkCommandVisible(capability, starlinkSnapshot))
            .ToList();
    }

    <div class="space-y-5">
        <section class="rounded-xl border border-cyan-500/20 bg-slate-950/60 p-4">
            <div class="flex flex-wrap items-center justify-between gap-3">
                <div class="min-w-0">
                    <span class="@GetStarlinkLiveBadgeClass(starlinkSnapshot)">@GetStarlinkStatusSummary(starlinkSnapshot)</span>
                    <p class="mt-3 text-sm text-slate-300">
                        Latency @FormatNullableMs(starlinkSnapshot.PopPingLatencyMs), obstruction @FormatNullablePercent(starlinkSnapshot.ObstructionPercent), alignment @FormatNullableDegrees(starlinkSnapshot.AlignmentErrorDegrees).
                    </p>
                </div>
                <div class="grid grid-cols-2 gap-x-8 gap-y-2 text-right text-sm">
                    <div>
                        <p class="text-xs text-slate-500">Adapter</p>
                        <p class="font-semibold text-white">@(_selectedStarlinkPath?.InterfaceName ?? "--")</p>
                    </div>
                    <div>
                        <p class="text-xs text-slate-500">XBond RTT</p>
                        <p class="font-semibold text-white">@FormatLatency(_selectedStarlinkPath?.RttMs)</p>
                    </div>
                    <div>
                        <p class="text-xs text-slate-500">Updated</p>
                        <p class="font-semibold text-white">@FormatLastUpdated(starlinkSnapshot.LastUpdatedUtc)</p>
                    </div>
                    <div>
                        <p class="text-xs text-slate-500">State</p>
                        <p class="font-semibold text-white">@FormatUnknown(starlinkSnapshot.DishState)</p>
                    </div>
                </div>
            </div>
        </section>

        <section class="grid grid-cols-2 gap-3">
            <div class="rounded-xl border border-slate-700 bg-slate-950/50 p-3">
                <p class="text-xs text-slate-400">Dish latency</p>
                <p class="mt-1 text-xl font-semibold text-white">@FormatNullableMs(starlinkSnapshot.PopPingLatencyMs)</p>
                <p class="mt-1 text-xs text-slate-500">Drop @FormatDropRate(starlinkSnapshot.PopPingDropRate)</p>
            </div>
            <div class="rounded-xl border border-slate-700 bg-slate-950/50 p-3">
                <p class="text-xs text-slate-400">Downlink</p>
                <p class="mt-1 text-xl font-semibold text-cyan-200">@FormatNullableSpeed(starlinkSnapshot.DownlinkMbps)</p>
                <p class="mt-1 text-xs text-slate-500">Direct dish</p>
            </div>
            <div class="rounded-xl border border-slate-700 bg-slate-950/50 p-3">
                <p class="text-xs text-slate-400">Uplink</p>
                <p class="mt-1 text-xl font-semibold text-pink-200">@FormatNullableSpeed(starlinkSnapshot.UplinkMbps)</p>
                <p class="mt-1 text-xs text-slate-500">Direct dish</p>
            </div>
            <div class="rounded-xl border border-slate-700 bg-slate-950/50 p-3">
                <p class="text-xs text-slate-400">Obstruction</p>
                <p class="mt-1 text-xl font-semibold text-amber-200">@FormatNullablePercent(starlinkSnapshot.ObstructionPercent)</p>
                <p class="mt-1 text-xs text-slate-500">@FormatObstructionState(starlinkSnapshot.CurrentlyObstructed)</p>
            </div>
            <div class="rounded-xl border border-slate-700 bg-slate-950/50 p-3">
                <p class="text-xs text-slate-400">Alignment</p>
                <p class="mt-1 text-xl font-semibold text-emerald-200">@FormatNullableDegrees(starlinkSnapshot.AlignmentErrorDegrees)</p>
                <p class="mt-1 text-xs text-slate-500">Az @FormatNullableDegrees(starlinkSnapshot.BoresightAzimuthDegrees)</p>
            </div>
            <div class="rounded-xl border border-slate-700 bg-slate-950/50 p-3">
                <p class="text-xs text-slate-400">Elevation</p>
                <p class="mt-1 text-xl font-semibold text-white">@FormatNullableDegrees(starlinkSnapshot.BoresightElevationDegrees)</p>
                <p class="mt-1 text-xs text-slate-500">Boresight</p>
            </div>
            <div class="rounded-xl border border-slate-700 bg-slate-950/50 p-3">
                <p class="text-xs text-slate-400">GPS</p>
                <p class="mt-1 text-xl font-semibold text-white">@FormatGpsState(starlinkSnapshot)</p>
                <p class="mt-1 text-xs text-slate-500">@FormatGpsSatellites(starlinkSnapshot.GpsSatellites)</p>
            </div>
            <div class="rounded-xl border border-slate-700 bg-slate-950/50 p-3">
                <p class="text-xs text-slate-400">Uptime</p>
                <p class="mt-1 text-xl font-semibold text-white">@FormatDuration(starlinkSnapshot.UptimeSeconds)</p>
                <p class="mt-1 text-xs text-slate-500">@FormatShortDeviceId(starlinkSnapshot.DeviceId)</p>
            </div>
        </section>

        @if (!string.IsNullOrWhiteSpace(starlinkSnapshot.Error))
        {
            <div class="rounded-xl border border-amber-500/25 bg-amber-500/10 p-3 text-sm text-amber-100">
                <i class="fas fa-triangle-exclamation mr-2 text-amber-300"></i>
                @starlinkSnapshot.Error
            </div>
        }

        <section class="grid grid-cols-1 gap-3 md:grid-cols-2">
            <StarlinkTelemetryChart Samples="@starlinkHistory" Title="Latency" Metric="latency" Unit="ms" Stroke="#22d3ee" />
            <StarlinkTelemetryChart Samples="@starlinkHistory" Title="Downlink" Metric="download" Unit="Mbps" Stroke="#38bdf8" />
            <StarlinkTelemetryChart Samples="@starlinkHistory" Title="Uplink" Metric="upload" Unit="Mbps" Stroke="#f472b6" />
            <StarlinkTelemetryChart Samples="@starlinkHistory" Title="Obstruction" Metric="obstruction" Unit="%" Stroke="#f59e0b" />
        </section>

        @if (starlinkSnapshot.ActiveAlerts.Count > 0)
        {
            <section class="rounded-xl border border-amber-500/25 bg-amber-500/10 p-4">
                <h4 class="font-semibold text-amber-100">Active Alerts</h4>
                <div class="mt-3 flex flex-wrap gap-2">
                    @foreach (var alert in starlinkSnapshot.ActiveAlerts)
                    {
                        <span class="rounded-full border border-amber-500/30 bg-amber-500/15 px-2.5 py-1 text-xs font-medium text-amber-100" title="@GetStarlinkAlertDescription(alert)">
                            @GetStarlinkAlertLabel(alert)
                        </span>
                    }
                </div>
            </section>
        }

        <section class="rounded-xl border border-slate-700 bg-slate-950/50">
            <div class="border-b border-slate-700 p-4">
                <div class="flex items-start justify-between gap-3">
                    <div>
                        <h4 class="font-semibold text-white">Starlink Actions</h4>
                        <p class="mt-1 text-sm text-slate-400">Open the local Starlink app or swipe an action fully right to run it from XNetwork.</p>
                    </div>
                    <span class="text-xs text-slate-500">@FormatLastUpdated(starlinkCapabilities.LastUpdatedUtc)</span>
                </div>
            </div>

            @if (_lastStarlinkCommandResult is not null)
            {
                <div class="@GetStarlinkCommandResultClass(_lastStarlinkCommandResult)">
                    <i class="@(_lastStarlinkCommandResult.Success ? "fas fa-check-circle" : "fas fa-triangle-exclamation")"></i>
                    <div>
                        <p class="font-semibold">@_lastStarlinkCommandResult.Message</p>
                        <p class="text-xs opacity-80">@FormatLastUpdated(_lastStarlinkCommandResult.CompletedUtc)</p>
                    </div>
                </div>
            }

            <div class="space-y-3 p-4">
                @foreach (var capability in openCapabilities)
                {
                    <a href="@capability.ActionUrl"
                       target="_blank"
                       rel="noopener noreferrer"
                       class="flex items-center justify-between rounded-xl border border-cyan-500/25 bg-cyan-500/10 px-4 py-3 text-sm font-semibold text-cyan-100 transition-colors hover:border-cyan-400/40 hover:bg-cyan-500/15">
                        <span class="inline-flex items-center gap-2">
                            <i class="fas fa-arrow-up-right-from-square"></i>
                            @GetStarlinkOpenActionLabel(capability)
                        </span>
                        <span class="text-xs font-medium text-cyan-100/70">@StarlinkTelemetrySettings.Host</span>
                    </a>
                }

                @foreach (var capability in commandCapabilities)
                {
                    <div class="@GetStarlinkActionCardClass(capability)">
                        <div class="flex items-start gap-3">
                            <span class="@GetStarlinkActionIconClass(capability)">
                                <i class="fas @GetStarlinkCommandIconClass(capability)"></i>
                            </span>
                            <div class="min-w-0 flex-grow">
                                <div class="flex flex-wrap items-center gap-2">
                                    <h5 class="font-semibold text-white">@capability.Label</h5>
                                    @if (capability.IsDisruptive)
                                    {
                                        <span class="rounded-full border border-amber-500/25 bg-amber-500/10 px-2 py-0.5 text-[11px] font-semibold text-amber-200">Disruptive</span>
                                    }
                                </div>
                                <p class="mt-1 text-sm leading-snug text-slate-400">@GetStarlinkCommandShortDescription(capability)</p>
                            </div>
                        </div>
                        <div class="mt-3">
                            <SlideToConfirm Label="@GetStarlinkCommandSlideLabel(capability)"
                                            BusyLabel="@GetStarlinkCommandBusyLabel(capability)"
                                            IconClass="@GetStarlinkCommandIconClass(capability)"
                                            Tone="@GetStarlinkCommandTone(capability)"
                                            Disabled="@(!capability.IsActionable || _starlinkCommandInProgress)"
                                            IsBusy="@IsStarlinkCommandBusy(capability)"
                                            OnConfirmed="() => ExecuteStarlinkCommandAsync(capability)" />
                        </div>
                    </div>
                }

                @if (openCapabilities.Count == 0 && commandCapabilities.Count == 0)
                {
                    <div class="rounded-xl border border-slate-700 bg-slate-900/60 p-3 text-sm text-slate-400">
                        No direct Starlink actions were detected from the current firmware.
                    </div>
                }
            </div>
        </section>

        <section class="grid grid-cols-1 gap-3 rounded-xl border border-slate-700 bg-slate-950/50 p-4 text-sm md:grid-cols-3">
            <div>
                <p class="text-xs text-slate-500">Hardware</p>
                <p class="font-medium text-slate-200">@FormatUnknown(starlinkSnapshot.HardwareVersion)</p>
            </div>
            <div>
                <p class="text-xs text-slate-500">Software</p>
                <p class="font-medium text-slate-200">@FormatUnknown(starlinkSnapshot.SoftwareVersion)</p>
            </div>
            <div>
                <p class="text-xs text-slate-500">Management</p>
                <p class="font-medium text-slate-200">@StarlinkTelemetrySettings.Host</p>
            </div>
        </section>
    </div>
</ActionSheet>

@code {
    private const ulong StaleAckWarningMs = 5_000;

    private readonly PeriodicTimer _timer = new(TimeSpan.FromSeconds(1));
    private readonly CancellationTokenSource _refreshCts = new();
    private XBondStatsSnapshot? _snapshot;
    private StarlinkTelemetrySnapshot _starlink = StarlinkTelemetrySnapshot.Unavailable();
    private bool _showStarlinkDetails;
    private XBondPathStatsSnapshot? _selectedStarlinkPath;
    private StarlinkCapability? _pendingStarlinkCommand;
    private StarlinkCommandResult? _lastStarlinkCommandResult;
    private bool _starlinkCommandInProgress;
    private IJSObjectReference? _animatedUiModule;
    private ElementReference _adapterListElement;
    private bool _adapterListAnimationEnabled;
    private bool _disposed;

    protected override async Task OnInitializedAsync()
    {
        await RefreshAsync();
        _ = RunRefreshLoopAsync();
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (_disposed || _adapterListAnimationEnabled || _snapshot?.DashboardPaths.Any() != true)
        {
            return;
        }

        try
        {
            _animatedUiModule ??= await JSRuntime.InvokeAsync<IJSObjectReference>("import", "./js/animatedUi.js");
            await _animatedUiModule.InvokeVoidAsync("enableAutoAnimate", _adapterListElement);
            _adapterListAnimationEnabled = true;
        }
        catch (JSDisconnectedException)
        {
        }
        catch (InvalidOperationException)
        {
        }
    }

    private async Task RunRefreshLoopAsync()
    {
        try
        {
            while (await _timer.WaitForNextTickAsync(_refreshCts.Token))
            {
                await InvokeAsync(RefreshAsync);
            }
        }
        catch (OperationCanceledException)
        {
        }
    }

    private async Task RefreshAsync()
    {
        if (_disposed)
        {
            return;
        }

        _snapshot = await XBondSnapshotCache.GetSnapshotAsync(_refreshCts.Token);
        _starlink = StarlinkTelemetryService.GetSnapshot();
        StateHasChanged();
    }

    private string StateLabel => _snapshot switch
    {
        null => "Loading",
        { HasError: true } => "Needs attention",
        { IsRunning: true } => "XBond active",
        _ => "Stopped"
    };

    private string ConnectionTitle => _snapshot switch
    {
        null => "Initializing Connection",
        _ => _snapshot.ConnectionTitle
    };

    private void OnPathCardClick(XBondPathStatsSnapshot path)
    {
        if (!IsStarlinkPath(path))
        {
            return;
        }

        _selectedStarlinkPath = path;
        _showStarlinkDetails = true;
    }

    private Task CloseStarlinkDetailsAsync()
    {
        _showStarlinkDetails = false;
        _pendingStarlinkCommand = null;
        return Task.CompletedTask;
    }

    private async Task ExecuteStarlinkCommandAsync(StarlinkCapability capability)
    {
        if (!capability.IsActionable || string.IsNullOrWhiteSpace(capability.DirectCommand) || _starlinkCommandInProgress)
        {
            return;
        }

        _pendingStarlinkCommand = capability;
        _starlinkCommandInProgress = true;
        _lastStarlinkCommandResult = null;
        await InvokeAsync(StateHasChanged);

        try
        {
            _lastStarlinkCommandResult = await StarlinkTelemetryService.ExecuteCommandAsync(
                capability.DirectCommand,
                _refreshCts.Token);
        }
        catch (OperationCanceledException)
        {
            _lastStarlinkCommandResult = new StarlinkCommandResult
            {
                Command = capability.DirectCommand,
                Success = false,
                Message = $"{capability.Label} command was canceled"
            };
        }
        finally
        {
            _starlinkCommandInProgress = false;
            _pendingStarlinkCommand = null;
            await InvokeAsync(StateHasChanged);
        }
    }

    private bool IsStarlinkPath(XBondPathStatsSnapshot path)
    {
        return StarlinkAdapterDetector.HasStarlinkMetadata(path, StarlinkTelemetrySettings.AdapterNameHints) ||
               StarlinkAdapterDetector.MatchesManagementGateway(path.Gateway, StarlinkTelemetrySettings.Host);
    }

    private RenderFragment StarlinkTelemetryInline(XBondPathStatsSnapshot path) => @<div class="mt-3 border-t border-slate-700/60 pt-3">
        @if (_starlink.IsAvailable)
        {
            var alertPreview = _starlink.ActiveAlerts.Take(2).ToList();
            var hiddenAlertCount = Math.Max(0, _starlink.ActiveAlerts.Count - alertPreview.Count);

            <div class="flex flex-wrap items-center gap-x-3 gap-y-2 text-xs text-slate-300">
                <span class="inline-flex items-center gap-1.5" title="Direct Starlink dish latency">
                    <i class="fas fa-satellite-dish text-cyan-300"></i>
                    <span>Dish @FormatNullableMs(_starlink.PopPingLatencyMs)</span>
                </span>
                <span class="inline-flex items-center gap-1.5" title="Direct Starlink dish downlink throughput">
                    <i class="fas fa-arrow-down text-cyan-300"></i>
                    <span>@FormatNullableSpeed(_starlink.DownlinkMbps)</span>
                </span>
                <span class="inline-flex items-center gap-1.5" title="Direct Starlink dish uplink throughput">
                    <i class="fas fa-arrow-up text-pink-300"></i>
                    <span>@FormatNullableSpeed(_starlink.UplinkMbps)</span>
                </span>
                <span class="inline-flex items-center gap-1.5" title="Direct Starlink obstruction estimate">
                    <i class="fas fa-ban text-amber-300"></i>
                    <span>Obs @FormatNullablePercent(_starlink.ObstructionPercent)</span>
                </span>
                <span class="inline-flex items-center gap-1.5" title="Direct Starlink alignment error">
                    <i class="fas fa-crosshairs text-emerald-300"></i>
                    <span>Align @FormatNullableDegrees(_starlink.AlignmentErrorDegrees)</span>
                </span>
                <span class="inline-flex items-center gap-1.5" title="Direct Starlink dish uptime">
                    <i class="far fa-hourglass-half text-slate-400"></i>
                    <span>Up @FormatDuration(_starlink.UptimeSeconds)</span>
                </span>
                @if (_starlink.GpsSatellites is not null)
                {
                    <span class="inline-flex items-center gap-1.5" title="Direct Starlink GPS satellites">
                        <i class="fas fa-location-crosshairs text-slate-400"></i>
                        <span>GPS @_starlink.GpsSatellites</span>
                    </span>
                }
            </div>

            @if (alertPreview.Count > 0)
            {
                <div class="mt-2 flex flex-wrap items-center gap-1.5">
                    @foreach (var alert in alertPreview)
                    {
                        <span class="rounded-full border border-amber-500/20 bg-amber-500/10 px-2 py-0.5 text-[11px] text-amber-200" title="@GetStarlinkAlertDescription(alert)">
                            @GetStarlinkAlertLabel(alert)
                        </span>
                    }
                    @if (hiddenAlertCount > 0)
                    {
                        <span class="rounded-full border border-slate-600/40 bg-slate-700/50 px-2 py-0.5 text-[11px] text-slate-300">
                            +@hiddenAlertCount
                        </span>
                    }
                </div>
            }
        }
        else
        {
            <div class="inline-flex items-center gap-2 text-xs text-amber-200" title="@_starlink.Error">
                <i class="fas fa-satellite-dish"></i>
                <span>Starlink stats unavailable</span>
            </div>
        }
    </div>;

    private RenderFragment<XBondPathStatsSnapshot> PathCard => path => @<article @key="path.PathId"
        class="@GetPathCardClass(path)"
        @onclick="() => OnPathCardClick(path)">
        <div class="flex items-center justify-between gap-3 text-sm">
            <div class="flex min-w-0 flex-grow items-center gap-3">
                @if (path.InterfaceUp)
                {
                    <div class="flex h-6 w-10 flex-shrink-0 items-end gap-0.5" aria-hidden="true">
                        @for (var i = 0; i < 4; i++)
                        {
                            <div class="@GetSignalBarClass(path, i)"></div>
                        }
                    </div>
                }
                else
                {
                    <i class="fas fa-signal-slash w-8 flex-shrink-0 text-center text-lg text-red-400"></i>
                }
                <div class="flex min-w-0 flex-col">
                    <span class="truncate text-base font-medium text-white">@path.Name</span>
                    <span class="truncate text-sm text-slate-400">@path.InterfaceName</span>
                    @if (path.HasCellularTelemetry)
                    {
                        <span class="mt-1 inline-flex items-center gap-1.5 text-xs font-medium text-slate-300" title="F50 modem signal">
                            @if (!string.IsNullOrWhiteSpace(path.CellularGeneration))
                            {
                                <span class="rounded-full border border-cyan-400/25 bg-cyan-400/10 px-1.5 py-0.5 text-cyan-100">@path.CellularGeneration</span>
                            }
                            @if (path.CellularSignalBars.HasValue)
                            {
                                <span class="inline-flex h-3 items-end gap-0.5" aria-label="@($"{path.CellularSignalBars.Value} signal bars")">
                                    @for (var i = 0; i < 5; i++)
                                    {
                                        <span class="@GetF50SignalBarClass(path, i)"></span>
                                    }
                                </span>
                            }
                        </span>
                    }
                </div>
            </div>
            <div class="flex flex-shrink-0 items-center gap-2">
                @if (GetAdapterAdminUrl(path) is { Length: > 0 } adminUrl)
                {
                    <a href="@adminUrl"
                       target="_blank"
                       rel="noopener noreferrer"
                       class="flex h-8 w-8 items-center justify-center rounded-lg border border-slate-600/50 bg-slate-700/50 text-slate-300 transition-colors hover:border-slate-500 hover:bg-slate-600/50 hover:text-white"
                       title="@($"Open {path.Name} admin page")"
                       @onclick:stopPropagation="true">
                        <i class="fas fa-arrow-up-right-from-square text-xs"></i>
                    </a>
                }
                <button type="button"
                        class="@GetStatusDotButtonClass(path)"
                        title="@GetPathStatusTooltip(path)"
                        aria-label="@GetPathStatusTooltip(path)"
                        @onclick:stopPropagation="true">
                    <span class="@GetStatusDotClass(path)" aria-hidden="true"></span>
                    <span class="adapter-status-popover" role="tooltip">@GetPathStatusTooltip(path)</span>
                </button>
            </div>
        </div>

        @if (path.InterfaceUp || path.IsActive)
        {
            var downloadMbps = path.DownloadMbps;
            var uploadMbps = path.UploadMbps;

            <div class="mt-3 flex flex-wrap items-center gap-x-4 gap-y-2 text-sm">
                <div class="flex items-center gap-1.5" title="@DownloadTitle(path)">
                    <i class="fas fa-arrow-down text-sm text-cyan-400"></i>
                    <AnimatedNumber Value="@SpeedFormatter.GetDisplayValue(downloadMbps)" Decimals="@SpeedFormatter.GetDisplayDecimals(downloadMbps)" CssClass="font-semibold text-white" AriaLabel="@SpeedFormatter.FormatSpeed(downloadMbps)" />
                    <span class="text-xs text-slate-400">@SpeedFormatter.GetSpeedUnit(downloadMbps)</span>
                </div>
                <div class="flex items-center gap-1.5">
                    <i class="fas fa-arrow-up text-sm text-pink-400"></i>
                    <AnimatedNumber Value="@SpeedFormatter.GetDisplayValue(uploadMbps)" Decimals="@SpeedFormatter.GetDisplayDecimals(uploadMbps)" CssClass="font-semibold text-white" AriaLabel="@SpeedFormatter.FormatSpeed(uploadMbps)" />
                    <span class="text-xs text-slate-400">@SpeedFormatter.GetSpeedUnit(uploadMbps)</span>
                </div>
                <div class="flex items-center gap-1.5">
                    <i class="far fa-clock text-sm text-slate-400"></i>
                    @if (path.RttMs.HasValue)
                    {
                        <AnimatedNumber Value="@Math.Round(path.RttMs.Value)" Decimals="0" CssClass="font-semibold text-white" AriaLabel="@FormatLatency(path.RttMs)" />
                        <span class="text-xs text-slate-400">ms</span>
                    }
                    else
                    {
                        <span class="font-semibold text-slate-400" aria-label="Latency unavailable">--</span>
                    }
                </div>
                <div class="flex items-center gap-1.5">
                    <i class="fas fa-triangle-exclamation text-xs text-slate-500"></i>
                    <AnimatedNumber Value="@Math.Round(path.LossPercent, 1)" Decimals="@GetPercentDecimals(path.LossPercent)" CssClass="@GetLossTextClass(path)" AriaLabel="@FormatPercent(path.LossPercent)" />
                    <span class="text-xs text-slate-400">loss</span>
                </div>
            </div>

            <div class="mt-3 flex flex-wrap gap-x-3 gap-y-1 text-xs text-slate-400">
                <span>@RoleLabel(path)</span>
                <span>Score @Math.Round(path.Score)</span>
                <span>Late @FormatPercent(path.LatePercent)</span>
                <span>Queue @path.QueueDepth</span>
            </div>

            @if (IsStarlinkPath(path))
            {
                @StarlinkTelemetryInline(path)
            }
        }
        else
        {
            <div class="mt-3 flex flex-wrap gap-x-3 gap-y-1 text-xs text-slate-400">
                <span>@path.StateText</span>
                <span>Score @Math.Round(path.Score)</span>
            </div>
        }
    </article>;

    private int GetLatencyValue()
    {
        if (_snapshot is null || _snapshot.EffectiveRttMs <= 0)
        {
            return 0;
        }

        return (int)Math.Round(_snapshot.EffectiveRttMs);
    }

    private double? GetAnchorDownloadMbps()
    {
        var anchor = _snapshot?.ActivePaths.FirstOrDefault(path => path.IsAnchor);
        return anchor?.DownloadMbps;
    }

    private double? GetBackupDownloadMbps()
    {
        var backup = _snapshot?.ActivePaths.FirstOrDefault(path => !path.IsAnchor);
        return backup?.DownloadMbps;
    }

    private bool IsConnectionStable()
    {
        return _snapshot?.IsStable == true;
    }

    private int GetLivePathCount()
    {
        return _snapshot?.DashboardPaths.Count(path => path.InterfaceUp) ?? 0;
    }

    private string GetServerIconClass()
    {
        return _snapshot switch
        {
            { HasError: true } => "flex h-10 w-10 flex-shrink-0 items-center justify-center rounded-full border border-red-500/30 bg-red-500/10 text-red-300",
            { IsRunning: true } => "flex h-10 w-10 flex-shrink-0 items-center justify-center rounded-full border border-green-500/30 bg-green-500/10 text-green-400",
            _ => "flex h-10 w-10 flex-shrink-0 items-center justify-center rounded-full bg-slate-700 text-slate-500"
        };
    }

    private string GetServerBadgeClass()
    {
        return _snapshot switch
        {
            { HasError: true } => "flex-shrink-0 rounded-full border border-red-500/30 bg-red-500/10 px-2 py-0.5 text-xs font-medium text-red-200",
            { IsRunning: true } => "flex-shrink-0 rounded-full border border-emerald-500/30 bg-emerald-500/10 px-2 py-0.5 text-xs font-medium text-emerald-200",
            _ => "flex-shrink-0 rounded-full border border-slate-600 bg-slate-700/40 px-2 py-0.5 text-xs font-medium text-slate-300"
        };
    }

    private string GetPathCardClass(XBondPathStatsSnapshot path)
    {
        const string baseClass = "rounded-xl border px-3 py-3 shadow-lg relative";
        var interactionClass = IsStarlinkPath(path)
            ? " cursor-pointer transition-colors hover:bg-slate-700/30 focus-within:ring-2 focus-within:ring-cyan-500/40"
            : "";

        if (!path.InterfaceUp)
        {
            return $"{baseClass} border-slate-700 bg-slate-800/45 opacity-80{interactionClass}";
        }

        if (path.InCooldown)
        {
            return $"{baseClass} border-amber-500/25 bg-amber-500/10{interactionClass}";
        }

        if (path.LossPercent >= 10 || path.RttMs >= 180)
        {
            return $"{baseClass} border-yellow-500/25 bg-yellow-500/5{interactionClass}";
        }

        return $"{baseClass} border-slate-700 bg-slate-800/50{interactionClass}";
    }

    private static string GetStatusDotButtonClass(XBondPathStatsSnapshot path)
    {
        var statusClass = GetPathStatusKind(path) switch
        {
            "connected" => "",
            "connecting" => "adapter-status-dot-connecting",
            "disconnecting" => "",
            "disconnected" => "",
            _ => ""
        };

        return $"adapter-status-dot-button {statusClass}".Trim();
    }

    private static string GetStatusDotClass(XBondPathStatsSnapshot path)
    {
        return GetPathStatusKind(path) switch
        {
            "connected" => "adapter-status-dot adapter-status-dot-connected",
            "connecting" => "adapter-status-dot adapter-status-dot-connecting-inner",
            "disconnecting" => "adapter-status-dot adapter-status-dot-disconnecting",
            "disconnected" => "adapter-status-dot adapter-status-dot-disconnected",
            _ => "adapter-status-dot adapter-status-dot-unknown"
        };
    }

    private static string GetPathStatusKind(XBondPathStatsSnapshot path)
    {
        if (!path.InterfaceUp || path.LossPercent >= 99.5)
        {
            return "disconnected";
        }

        if (path.InCooldown || path.LossPercent >= 10 || path.StaleAckMs is >= StaleAckWarningMs)
        {
            return "disconnecting";
        }

        if (path.IsConfigured && path.RttMs is null && !path.IsActive)
        {
            return "connecting";
        }

        return path.IsConfigured ? "connected" : "unknown";
    }

    private static string GetPathStatusTooltip(XBondPathStatsSnapshot path)
    {
        if (!path.InterfaceUp)
        {
            return "No carrier";
        }

        if (path.LossPercent >= 99.5)
        {
            return string.IsNullOrWhiteSpace(path.DemotionReason) ? "Path has 100% heartbeat loss" : path.DemotionReason;
        }

        if (path.InCooldown)
        {
            return string.IsNullOrWhiteSpace(path.DemotionReason) ? "Cooldown" : path.DemotionReason;
        }

        if (path.LossPercent >= 10)
        {
            return string.IsNullOrWhiteSpace(path.DemotionReason)
                ? $"High heartbeat loss ({FormatPercent(path.LossPercent)})"
                : path.DemotionReason;
        }

        if (path.StaleAckMs is >= StaleAckWarningMs)
        {
            return "Heartbeat ACK is stale";
        }

        if (!path.IsConfigured)
        {
            return "Available, not in XBond";
        }

        return path.IsActive ? $"Active {RoleLabel(path)} path" : "Standby path";
    }

    private static string GetSignalBarClass(XBondPathStatsSnapshot path, int index)
    {
        var heightClass = index switch
        {
            0 => "h-2",
            1 => "h-3",
            2 => "h-4",
            3 => "h-6",
            _ => "h-2"
        };

        var isActive = index < GetSignalStrength(path);
        var colorClass = isActive ? GetSignalColorClass(path) : "bg-slate-700";
        var animationClass = GetPathStatusKind(path) == "connecting" && isActive ? "animate-pulse" : "";

        return $"{heightClass} {colorClass} {animationClass} w-1.5 rounded-sm transition-all";
    }

    private static int GetSignalStrength(XBondPathStatsSnapshot path)
    {
        if (!path.InterfaceUp)
        {
            return 0;
        }

        if (path.InCooldown || path.LossPercent >= 25 || path.RttMs >= 300)
        {
            return 1;
        }

        if (path.LossPercent >= 10 || path.RttMs >= 180)
        {
            return 2;
        }

        if (path.LossPercent >= 2 || path.RttMs >= 120)
        {
            return 3;
        }

        return 4;
    }

    private static string GetSignalColorClass(XBondPathStatsSnapshot path)
    {
        if (path.InCooldown || path.LossPercent >= 25 || path.RttMs >= 300)
        {
            return "bg-red-400";
        }

        if (path.LossPercent >= 10 || path.RttMs >= 180)
        {
            return "bg-yellow-400";
        }

        if (path.LossPercent >= 2 || path.RttMs >= 120)
        {
            return "bg-cyan-400";
        }

        return path.IsActive ? "bg-emerald-400" : "bg-cyan-400";
    }

    private static string GetF50SignalBarClass(XBondPathStatsSnapshot path, int index)
    {
        var heightClass = index switch
        {
            0 => "h-1.5",
            1 => "h-2",
            2 => "h-2.5",
            3 => "h-3",
            _ => "h-3.5"
        };
        var isActive = index < path.CellularSignalBars.GetValueOrDefault();
        return $"{heightClass} {(isActive ? "bg-emerald-300" : "bg-slate-700")} w-1 rounded-sm transition-all";
    }

    private static string RoleLabel(XBondPathStatsSnapshot path)
    {
        if (!path.IsConfigured)
        {
            return "not in XBond";
        }

        if (path.IsActive && !path.IsAnchor && string.Equals(path.Role, "probe", StringComparison.OrdinalIgnoreCase))
        {
            return "backup";
        }

        return path.Role;
    }

    private string? GetAdapterAdminUrl(XBondPathStatsSnapshot path)
    {
        if (IsStarlinkPath(path))
        {
            return null;
        }

        var proxy = LocalDeviceProxyService.FindAdminProxyForAdapter(path.InterfaceName, path.Name, path.Gateway);
        return proxy is null
            ? null
            : LocalDeviceProxyService.BuildLocalProxyUrl(proxy, NavigationManager.ToAbsoluteUri(NavigationManager.Uri));
    }

    private static string GetLossTextClass(XBondPathStatsSnapshot path)
    {
        return path.LossPercent switch
        {
            >= 10 => "font-semibold text-red-300",
            >= 2 => "font-semibold text-yellow-300",
            _ => "font-semibold text-white"
        };
    }

    private static int GetPercentDecimals(double value)
    {
        return Math.Abs(value - Math.Round(value)) < 0.05 ? 0 : 1;
    }

    private static string FormatLatency(double? value)
    {
        return value.HasValue && value.Value > 0 ? $"{Math.Round(value.Value)} ms" : "--";
    }

    private static string FormatPercent(double? value)
    {
        return value.HasValue ? $"{Math.Round(value.Value, 1)}%" : "--";
    }

    private static string FormatDegrees(double? value)
    {
        return value.HasValue ? $"{Math.Round(value.Value, 1)} deg" : "--";
    }

    private static string FormatMode(string? mode)
    {
        if (string.IsNullOrWhiteSpace(mode))
        {
            return "XBond";
        }

        return mode.Replace('-', ' ');
    }

    private static string DownloadTitle(XBondPathStatsSnapshot path)
    {
        return path.DuplicateDownloadMbps > 0
            ? $"Raw adapter receive: {SpeedFormatter.FormatSpeed(path.DownloadMbps)}. Useful tunnel receive: {SpeedFormatter.FormatSpeed(path.UsefulDownloadMbps)}. Duplicate receive: {SpeedFormatter.FormatSpeed(path.DuplicateDownloadMbps)}."
            : $"Useful tunnel receive: {SpeedFormatter.FormatSpeed(path.UsefulDownloadMbps)}.";
    }

    private string GetStarlinkSheetTitle()
    {
        return string.IsNullOrWhiteSpace(_selectedStarlinkPath?.Name) ? "Starlink" : _selectedStarlinkPath.Name;
    }

    private string? GetStarlinkSheetDescription()
    {
        return _selectedStarlinkPath is null
            ? "Direct dish telemetry"
            : $"{_selectedStarlinkPath.InterfaceName} - direct dish telemetry";
    }

    private static string GetStarlinkLiveBadgeClass(StarlinkTelemetrySnapshot snapshot)
    {
        return snapshot.IsAvailable
            ? "inline-flex items-center rounded-full border border-emerald-500/30 bg-emerald-500/10 px-2.5 py-1 text-xs font-semibold text-emerald-200"
            : "inline-flex items-center rounded-full border border-amber-500/30 bg-amber-500/10 px-2.5 py-1 text-xs font-semibold text-amber-200";
    }

    private static string GetStarlinkStatusSummary(StarlinkTelemetrySnapshot snapshot)
    {
        if (!snapshot.IsAvailable)
        {
            return "Unavailable";
        }

        if (snapshot.CurrentlyObstructed == true)
        {
            return "Obstructed";
        }

        if (snapshot.ActiveAlerts.Count > 0)
        {
            return "Alert";
        }

        return "Live";
    }

    private static string FormatNullableMs(double? milliseconds)
    {
        return milliseconds is > 0 ? $"{Math.Round(milliseconds.Value)} ms" : "--";
    }

    private static string FormatNullableSpeed(double? speedMbps)
    {
        return speedMbps is >= 0 ? SpeedFormatter.FormatSpeed(speedMbps.Value) : "--";
    }

    private static string FormatNullablePercent(double? percent)
    {
        return percent is >= 0 ? $"{Math.Round(percent.Value, percent.Value < 1 ? 2 : 1)}%" : "--";
    }

    private static string FormatNullableDegrees(double? degrees)
    {
        return degrees is >= 0 ? $"{Math.Round(degrees.Value, 1)} deg" : "--";
    }

    private static string FormatDuration(long? totalSeconds)
    {
        if (totalSeconds is null or < 0)
        {
            return "--";
        }

        var duration = TimeSpan.FromSeconds(totalSeconds.Value);
        if (duration.TotalDays >= 1)
        {
            return $"{(int)duration.TotalDays}d {duration.Hours}h";
        }

        if (duration.TotalHours >= 1)
        {
            return $"{(int)duration.TotalHours}h {duration.Minutes}m";
        }

        return $"{Math.Max(1, duration.Minutes)}m";
    }

    private static string FormatDropRate(double? dropRate)
    {
        return dropRate is >= 0 ? $"{Math.Round(dropRate.Value * 100, 2)}%" : "--";
    }

    private static string FormatObstructionState(bool? currentlyObstructed)
    {
        return currentlyObstructed switch
        {
            true => "Currently obstructed",
            false => "Clear",
            _ => "Unknown"
        };
    }

    private static string FormatGpsState(StarlinkTelemetrySnapshot snapshot)
    {
        return snapshot.GpsValid switch
        {
            true => "Valid",
            false => "No fix",
            _ => "--"
        };
    }

    private static string FormatGpsSatellites(int? satellites)
    {
        return satellites.HasValue ? $"{satellites.Value} satellites" : "--";
    }

    private static string FormatLastUpdated(DateTimeOffset? updatedUtc)
    {
        if (updatedUtc is null)
        {
            return "--";
        }

        var age = DateTimeOffset.UtcNow - updatedUtc.Value;
        if (age.TotalSeconds < 60)
        {
            return $"{Math.Max(0, (int)age.TotalSeconds)}s ago";
        }

        if (age.TotalMinutes < 60)
        {
            return $"{(int)age.TotalMinutes}m ago";
        }

        return updatedUtc.Value.ToLocalTime().ToString("HH:mm");
    }

    private static string FormatShortDeviceId(string? deviceId)
    {
        if (string.IsNullOrWhiteSpace(deviceId))
        {
            return "--";
        }

        return deviceId.Length <= 12 ? deviceId : $"{deviceId[..6]}...{deviceId[^4..]}";
    }

    private static string FormatUnknown(string? value)
    {
        return string.IsNullOrWhiteSpace(value) ? "--" : value;
    }

    private static bool IsStarlinkCommandVisible(StarlinkCapability capability, StarlinkTelemetrySnapshot snapshot)
    {
        if (snapshot.HasActuators == false &&
            (capability.Key.Contains("stow", StringComparison.OrdinalIgnoreCase) ||
             capability.Key.Contains("unstow", StringComparison.OrdinalIgnoreCase)))
        {
            return false;
        }

        return true;
    }

    private static IReadOnlyList<StarlinkCapability> GetUniqueStarlinkOpenCapabilities(IReadOnlyList<StarlinkCapability> capabilities)
    {
        return capabilities
            .Where(capability => !string.IsNullOrWhiteSpace(capability.ActionUrl))
            .GroupBy(capability => capability.ActionUrl, StringComparer.OrdinalIgnoreCase)
            .Select(group => group.OrderBy(capability => capability.Key.Equals("web-ui", StringComparison.OrdinalIgnoreCase) ? 0 : 1).First())
            .ToArray();
    }

    private static string GetStarlinkOpenActionLabel(StarlinkCapability capability)
    {
        return capability.Key.Equals("web-ui", StringComparison.OrdinalIgnoreCase)
            ? "Open Starlink app"
            : capability.Label;
    }

    private static string GetStarlinkCommandShortDescription(StarlinkCapability capability)
    {
        return capability.DirectCommand switch
        {
            "reboot" => "Restart the terminal. Starlink may be offline for a few minutes.",
            "stow" => "Move the dish into stow position.",
            "unstow" => "Return the dish from stow position.",
            "dish_clear_obstruction_map" => "Clear learned obstruction history. It can take hours or days to rebuild.",
            _ => capability.Description
        };
    }

    private static string GetStarlinkCommandSlideLabel(StarlinkCapability capability)
    {
        return capability.DirectCommand switch
        {
            "dish_clear_obstruction_map" => "Slide to reset map",
            _ => $"Slide to {capability.Label.ToLowerInvariant()}"
        };
    }

    private static string GetStarlinkCommandBusyLabel(StarlinkCapability capability)
    {
        return capability.DirectCommand switch
        {
            "dish_clear_obstruction_map" => "Resetting map...",
            _ => $"{capability.Label}..."
        };
    }

    private static string GetStarlinkCommandTone(StarlinkCapability capability)
    {
        return capability.DirectCommand switch
        {
            "reboot" or "dish_clear_obstruction_map" => "danger",
            "stow" => "warning",
            _ => "info"
        };
    }

    private static string GetStarlinkCommandIconClass(StarlinkCapability capability)
    {
        return capability.DirectCommand switch
        {
            "reboot" => "fa-power-off",
            "stow" => "fa-box-archive",
            "unstow" => "fa-satellite-dish",
            "dish_clear_obstruction_map" => "fa-map-location-dot",
            _ => "fa-bolt"
        };
    }

    private static string GetStarlinkActionCardClass(StarlinkCapability capability)
    {
        return capability.DirectCommand switch
        {
            "reboot" or "dish_clear_obstruction_map" => "rounded-xl border border-red-500/25 bg-red-500/10 p-3",
            "stow" => "rounded-xl border border-amber-500/25 bg-amber-500/10 p-3",
            _ => "rounded-xl border border-cyan-500/20 bg-cyan-500/10 p-3"
        };
    }

    private static string GetStarlinkActionIconClass(StarlinkCapability capability)
    {
        return capability.DirectCommand switch
        {
            "reboot" or "dish_clear_obstruction_map" => "inline-flex h-8 w-8 flex-shrink-0 items-center justify-center rounded-lg bg-red-500/15 text-red-200",
            "stow" => "inline-flex h-8 w-8 flex-shrink-0 items-center justify-center rounded-lg bg-amber-500/15 text-amber-200",
            _ => "inline-flex h-8 w-8 flex-shrink-0 items-center justify-center rounded-lg bg-cyan-500/15 text-cyan-200"
        };
    }

    private bool IsStarlinkCommandBusy(StarlinkCapability capability)
    {
        return _starlinkCommandInProgress &&
               _pendingStarlinkCommand?.DirectCommand == capability.DirectCommand;
    }

    private static string GetStarlinkCommandResultClass(StarlinkCommandResult result)
    {
        return result.Success
            ? "flex items-start gap-3 border-b border-emerald-500/20 bg-emerald-500/10 p-4 text-sm text-emerald-100"
            : "flex items-start gap-3 border-b border-red-500/20 bg-red-500/10 p-4 text-sm text-red-100";
    }

    private static string GetStarlinkAlertLabel(string alert)
    {
        return alert switch
        {
            "obstruction map reset" => "Obstruction map reset",
            "slow ethernet" or "slow ethernet 100" => "Slow Ethernet",
            _ => alert
        };
    }

    private static string GetStarlinkAlertDescription(string alert)
    {
        return alert.ToLowerInvariant() switch
        {
            "obstruction map reset" => "The learned obstruction map was cleared and Starlink is rebuilding obstruction history.",
            "slow ethernet" or "slow ethernet 100" => "The Ethernet link is negotiating below the expected speed.",
            "obstructed" => "The dish currently sees an obstruction.",
            "roaming" => "Starlink reports it is roaming outside its registered service area.",
            "moving too fast" => "Starlink reports the terminal is moving faster than the active plan or hardware allows.",
            "thermal shutdown" => "Starlink has shut down because of temperature.",
            "thermal throttle" or "power thermal" => "Starlink is reducing performance because of temperature or power constraints.",
            "low motor current" => "Starlink reported a motor current issue.",
            "signal lower than expected" => "Starlink reports signal quality below the expected level.",
            "telemetry stale" => "XNetwork has not received fresh Starlink telemetry recently.",
            _ => alert
        };
    }

    public async ValueTask DisposeAsync()
    {
        _disposed = true;
        await _refreshCts.CancelAsync();
        _timer.Dispose();

        if (_animatedUiModule != null)
        {
            try
            {
                await _animatedUiModule.InvokeVoidAsync("disableAutoAnimate", _adapterListElement);
                await _animatedUiModule.DisposeAsync();
            }
            catch (JSDisconnectedException)
            {
            }
        }

        _refreshCts.Dispose();
    }
}
